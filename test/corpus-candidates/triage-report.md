# Candidate triage report

Generated by `scripts/triage-candidates.ts` over 652 candidates. Not committed as fixtures — this drives the promotion step.

## Buckets

| outcome | count |
|---|---:|
| `canonical` | 309 |
| `noncanonical` | 175 |
| `rejects-syntax` | 123 |
| `rejects-semantic` | 45 |
| **intent mismatches** | 24 |
| **parser/formatter bugs** | 0 |

## ⚠️ Intent mismatches — the parser disagrees with the author guess; a reviewer decides which is right

- `cand.resources.image.kindless-no-attr-invalid` (§4.2) — tolerated surface
  source: `"<m>![a](i.jpg)</m>\n"`
  intent: a kindless image wrapper still needs at least one attribute — expect a semantic error

- `cand.inline.wrapper.attribute-invalid` (§10.2) — canonical
  source: `"<i lang=en>x</i>\n"`
  intent: attribute on an <i> wrapper — not allowed; expect literal text or error

- `cand.inline.mark.setext-like-literal` (§10.2) — syntax: unclosed inline token
  source: `"=== x\n"`
  intent: three equals at line start — mark delimiter needs a closer; ambiguous with setext-looking line; expect literal

- `cand.inline.deletion-sub.midline-with-space-invalid` (§10.11) — tolerated surface
  source: `"p ~~~a b~~~ r\n"`
  intent: ~~~ mid-line with a space in the sub child — atomic sub rule fails; expect syntax error

- `cand.inline.code.double-empty-invalid` (§10.9) — tolerated surface
  source: `"`` ``\n"`
  intent: empty code content with a double-backtick delimiter — expect syntax error

- `cand.blocks.heading.id-extra-content-invalid` (§5.1) — canonical
  source: `"# Title {#a} extra\n"`
  intent: text after the same-line ID suffix — expect the id not to be recognised or a syntax error

- `cand.blocks.heading.id-space-invalid` (§5.2) — syntax: invalid block id suffix
  source: `"# Title {#a b}\n"`
  intent: space inside the identifier — expect the id not to be a valid suffix

- `cand.blocks.hr.star-plus-content-literal` (§2.2) — syntax: unclosed inline token
  source: `"*** x\n"`
  intent: stars followed by content — not a valid HR; expect literal text

- `cand.blocks.priority.block-id-after-list` (§5.1) — canonical
  source: `"- item\n{#list-id}\n"`
  intent: id suffix after a list — ambiguous whether id attaches to the list node (addressable) or errors

- `cand.footnotes.definition.space-before-colon-literal` (§11.2) — semantic: footnote reference [^x] has no definition
  source: `"[^x] : note\n"`
  intent: space between the bracket and the colon — not a valid definition; expect literal text

- `cand.footnotes.definition.content-leading-space-normalize` (§11.2) — tolerated surface
  source: `"[^a]:  leading\n"`
  intent: extra space after the colon — normal mode collapses to exactly one; strict rejects

- `cand.lists.task.invalid-character-invalid` (§9.3.2) — canonical
  source: `"- [y] no\n"`
  intent: only [ ], [x], [X] are task markers — [y] is not; expect literal bracket text or error

- `cand.lists.indent.one-space-ambiguous` (§9.4) — syntax: list must start at level 1
  source: `"- a\n - b\n"`
  intent: one-space indent is not a valid level-2 — ambiguous continuation; expect it to join the item as a continuation

- `cand.lists.structured.heading-invalid` (§9.8) — canonical
  source: `"- a\n\n  # Heading\n"`
  intent: a heading is not in the permitted structured-child list — expect the heading to end the list or an error

- `cand.lists.structured.hr-invalid` (§9.8) — canonical
  source: `"- a\n\n  ---\n"`
  intent: an HR is not a permitted structured child of a list item — expect list end or error

- `cand.tables.rows.header-more-cells-invalid` (§12.3) — tolerated surface
  source: `"| A | B |\n| --- |\n"`
  intent: the separator has fewer cells than the header — expect a syntax error

- `cand.tables.rows.separator-more-cells-invalid` (§12.3) — tolerated surface
  source: `"| A |\n| --- | --- |\n"`
  intent: the separator has more cells than the header — expect a syntax error

- `cand.containers.lined.no-header-blank-after-content` (§2.7.1) — tolerated surface
  source: `"Exkurs\n___\nContent\n___\n"`
  intent: missing blank line after lined opener — normal mode inserts it; strict rejects

- `cand.containers.type.tab-invalid` (§2.7.4) — tolerated surface
  source: `"Bad\tType\n___\n\nContent\n___\n"`
  intent: tab (whitespace) in TYPE — excluded; expect syntax error

- `cand.containers.lined.typed-header-collision-table` (§2.7.1) — syntax: invalid TYPE character
  source: `"| A | B |\n___\n\nContent\n___\n"`
  intent: a table-looking header line before ___ — ambiguous; table/paragraph likely wins over lined container

- `cand.quotes.lone-greater-than` (§3.5) — semantic: empty quote region
  source: `">\n"`
  intent: a lone > is an empty quote line, never a closer — probe whether a region of only an empty line is valid

- `cand.quotes.heading-depth-two` (§3.10.1) — syntax: quote must start at depth 1
  source: `"> > ## Heading\n"`
  intent: a heading at quote depth 2 — valid

- `cand.quotes.id-before-attribution-invalid` (§3.22) — tolerated surface
  source: `"> Quote.\n{#q}\n-- Author\n"`
  intent: id before the attribution — wrong order; probe whether this is an error or the attribution becomes text

- `cand.quotes.attribution.empty-invalid` (§3.17) — tolerated surface
  source: `"> Quote.\n-- \n"`
  intent: an attribution with empty content — expect a syntax error

## canonical (303)

_promote as **canonical valid** fixtures._

- `cand.resources.image.block-lone-line` (§4.4) `"![](img.png)\n"` — a lone image line is a block ImageBlock, not a paragraph
- `cand.resources.image.inline-in-paragraph` (§4.4) `"before ![alt](img.png) after\n"` — an image with surrounding text is inline, not block
- `cand.resources.image.trailing-text-inline` (§4.4) `"![](img.png) after\n"` — an image line with trailing text — not a whole-line block; probe inline paragraph
- `cand.resources.image.two-images-line` (§4.4) `"![a](a.png) ![b](b.png)\n"` — two images on one line — not a block resource (whole-line rule); probe paragraph with two inline images
- `cand.resources.image.with-title-block` (§4.4) `"![Alt](img.jpg \"Title\")\n"` — a lone image line with a title — block ImageBlock with title
- `cand.resources.image.empty-dest-block` (§4.4) `"![](<>)\n"` — a lone image line with an empty destination — block with empty src
- `cand.resources.image.grouped-block` (§4.3) `"<m group=trip>![Alt](image.jpg)</m>\n"` — a kindless wrapper with group around a block image — valid block resource
- `cand.resources.image.grouped-inline` (§4.3) `"see <m group=trip>![a](i.jpg)</m> now\n"` — a grouped image with surrounding text is inline, not a block
- `cand.resources.image.grouped-caption` (§4.5) `"<m group=trip>![Alt](image.jpg)\\\n*Caption*</m>\n"` — a grouped block image with a caption inside the wrapper — valid
- `cand.resources.image.data-only` (§4.6) `"<m data-role=hero>![a](i.jpg)</m>\n"` — a kindless image wrapper with only data-* — valid
- `cand.resources.video.whole-line-block` (§4.4) `"<m video>[Video](film.mp4)</m>\n"` — a typed video occupying the whole line is a block resource
- `cand.resources.audio.block` (§4.4) `"<m audio>[Audio](sound.mp3)</m>\n"` — a whole-line audio resource is a block
- `cand.resources.embed.block` (§4.4) `"<m embed>[Demo](https://example.org)</m>\n"` — a whole-line embed resource is a block
- `cand.resources.download.block` (§4.4) `"<m download>[File](file.zip)</m>\n"` — a whole-line download is a block resource
- `cand.resources.download.inline` (§4.4) `"Get <m download>[File](file.zip)</m> now.\n"` — download may be inline — valid
- `cand.resources.download.inline-with-lang` (§4.6) `"Get <m download lang=de>[File](file.zip)</m> now.\n"` — download allows lang even inline — valid
- `cand.resources.typed.empty-label-ok` (§4.4) `"<m video>[](a.mp4)</m>\n"` — an empty link label inside a typed wrapper — permitted (empty label is not a link here)
- `cand.resources.typed.caption` (§4.5) `"<m video>[Talk](talk.mp4)\\\n*Recorded live*</m>\n"` — a typed block resource with a caption — valid
- `cand.resources.typed.caption-bold-content` (§4.5) `"<m video>[Talk](talk.mp4)\\\n*Recorded **live** in 2026*</m>\n"` — caption content parses the ordinary inline grammar — nested strong is allowed
- `cand.resources.caption.basic` (§4.5) `"![Alt](image.jpg)\\\n*Caption*\n"` — a bare block image with a caption — valid without a wrapper
- `cand.resources.caption.plain-text-not-caption` (§4.5) `"![Alt](image.jpg)\\\nplain text\n"` — the caption carrier must be a complete emphasis line — plain text after the hard break is not a caption
- `cand.resources.caption.double-emphasis-carrier` (§4.5) `"![Alt](image.jpg)\\\n**Caption**\n"` — a strong line after the hard break — probe whether ** is a valid caption carrier
- `cand.resources.attr.lang-data-mix` (§4.2) `"<m lang=en data-role=term>Text</m>\n"` — lang plus data-* on a kindless span — valid
- `cand.resources.attr.preview-quoted-space` (§4.6) `"<m video preview=\"poster image.jpg\">[V](v.mp4)</m>\n"` — preview uses destination semantics — a quoted value with a space is allowed
- `cand.resources.attr.bare-value-equals` (§4.7) `"<m data-x=a=b>Text</m>\n"` — '=' is allowed inside a bare value — probe that the first '=' splits key and value
- `cand.resources.attr.bare-value-slash` (§4.7) `"<m data-url=https://example.org/x>Text</m>\n"` — a bare value containing slashes and colons — valid
- `cand.resources.attr.video-all-canonical` (§4.6) `"<m video group=trip lang=de preview=poster.jpg data-role=hero>[V](v.mp4)</m>\n"` — video with every allowed attribute in canonical order — valid
- `cand.resources.metadata.lang-only` (§4.2) `"<m lang=de>Text</m>\n"` — a generic span with only lang — valid
- `cand.resources.wrapper.uppercase-m-literal` (§4.1) `"<M lang=en>Text</M>\n"` — uppercase M is not the MIB tag — expect literal text
- `cand.resources.image.inline-wrapper-in-paragraph` (§4.3) `"text <m data-role=fig>![a](i.jpg)</m> text\n"` — a data-wrapped inline image inside a paragraph — valid inline
- `cand.resources.embed.any-uri` (§4.4) `"<m embed>[Demo](mailto:user@x.org)</m>\n"` — an embed link with a mailto destination — the link grammar allows it; probe validation
- `cand.resources.video.group-lang-preview` (§4.6) `"<m video lang=de preview=p.jpg>[V](v.mp4)</m>\n"` — video with a subset of attributes — valid
- `cand.resources.image.alt-markup` (§4.3) `"![An *important* image](image.jpg)\n"` — alt content parses inline markup — valid block image
- `cand.resources.caption.after-grouped-image` (§4.5) `"<m group=x>![a](i.jpg)\\\n*Cap*</m>\n"` — caption on a grouped block image — valid
- `cand.resources.image.caption-with-id` (§5.1) `"![Alt](image.jpg)\\\n*Caption*\n{#img}\n"` — an id suffix after a block resource with a caption — attaches to the resource block
- `cand.resources.download.block-with-caption` (§4.5) `"<m download>[File](file.zip)\\\n*Download*</m>\n"` — a block download may carry a caption — valid
- `cand.inline.em.intraword-underscore-literal` (§10.11) `"snake_case_name\n"` — underscores within a word open no markup — expect all literal text
- `cand.inline.em.intraword-star-em` (§10.3) `"foo*bar*baz\n"` — a single star between word characters is an Em opener/closer (W-after rule differs from _) — expect Em around 'bar'
- `cand.inline.em.spaces-around-literal` (§10.3) `"a * b\n"` — star surrounded by spaces is neither opener nor closer — literal
- `cand.inline.strong.nested-em` (§10.2) `"**a *b* c**\n"` — Strong containing Em — valid and canonical
- `cand.inline.em.nested-strong` (§10.2) `"*a **b** c*\n"` — Em containing Strong — valid and canonical
- `cand.inline.em.star-dash-mix-literal` (§10.3) `"a *-* b\n"` — Em around a dash — valid; probes punctuation flanking
- `cand.inline.em.unicode-word` (§10.3) `"Über *em* fließt\n"` — unicode letters count as word characters — Em still parses between them
- `cand.inline.strong.underscore-intraword-literal` (§10.11) `"a__b__c\n"` — double underscore between word characters — no Strong; literal text
- `cand.inline.wrapper.intraword-valid` (§10.3) `"a<i>b</i>c\n"` — wrapper enables intra-word emphasis which * cannot express — valid, canonicalises to a*b*c
- `cand.inline.wrapper.uppercase-literal` (§10.2) `"<I>x</I>\n"` — uppercase wrapper tag — MIB tags are exact lowercase; expect literal text
- `cand.inline.wrapper.self-closing-literal` (§10.2) `"<i/>x\n"` — self-closing wrapper — not part of MIB grammar; expect literal text
- `cand.inline.wrapper.unknown-tag-literal` (§10.4) `"<div>x</div>\n"` — unknown HTML-like tag — no raw HTML passthrough; expect literal text
- `cand.inline.deletion.intraword` (§10.3) `"a~~b~~c\n"` — deletion between word characters — flanking rule W-before/P-after; ambiguous whether it parses or stays literal
- `cand.inline.obsolete.three-hyphens-literal` (§10.11) `"this is --- I think --- my opinion\n"` — three hyphens are never an Obsolete opener (em-dash ASCII) — expect literal text
- `cand.inline.sup.punctuation-content` (§10.11) `"x^(a+b)^\n"` — sup with punctuation-only content — valid atomic sup
- `cand.inline.sup.whitespace-literal` (§10.11) `"a^b c^d\n"` — whitespace inside the caret pair — no valid atomic sup; carets stay literal
- `cand.inline.sup.intraword` (§10.11) `"x^y^z\n"` — superscript between word characters — no flanking rule for ^; expect Sup
- `cand.inline.sub.intraword` (§10.11) `"a~b~c\n"` — subscript between word characters — no flanking rule; expect Sub
- `cand.inline.sub.whitespace-literal` (§10.11) `"a~b c~d\n"` — whitespace inside the tilde pair — no valid atomic sub; literal
- `cand.inline.sub.unclosed-literal` (§10.10) `"a~b\n"` — unclosed single tilde — stays literal text, even in strict mode
- `cand.inline.deletion-sub.midline-valid` (§10.11) `"p ~~~q~~~ r\n"` — mid-line ~~~ gives Deletion wrapping Sub — valid (block fence only at line start)
- `cand.inline.link.space-before-paren-literal` (§10.5.1) `"[x] (a)\n"` — a space between the label and the parens — not a direct link; expect literal text
- `cand.inline.link.ref-style-literal` (§1.2) `"[x][y]\n"` — reference-style link stays literal — not an error, never promoted
- `cand.inline.link.ref-style-collapsed-literal` (§1.2) `"[x][]\n"` — collapsed reference form stays literal text
- `cand.inline.link.bare-bracket-literal` (§1.2) `"[x]\n"` — a lone bracket pair stays literal text
- `cand.inline.link.bare-dest-trailing-paren-literal-part` (§10.5.1) `"[x](a) b\n"` — text after a valid link — the link parses, then space and b
- `cand.inline.link.label-code-valid` (§10.5.1) `"[`code` in label](a)\n"` — inline code inside a link label — valid
- `cand.inline.link.label-em-valid` (§10.5.1) `"[*em* label](a)\n"` — emphasis inside a link label — valid
- `cand.inline.link.label-image-valid` (§10.5.1) `"[![alt](i)](u)\n"` — a nested image inside a link label is allowed (image is not an anchor) — valid
- `cand.inline.link.fragment-id-dest` (§5.3) `"[Go](#section-2)\n"` — concrete document anchor destination — valid, href stored literally
- `cand.inline.link.symbolic-dest` (§10.5) `"[$sym]($sym)\n"` — symbolic internal target — valid, dollar preserved literally
- `cand.inline.autolink.http` (§13.2) `"<http://example.org>\n"` — http scheme autolink — valid
- `cand.inline.autolink.ftp-literal` (§13.3) `"<ftp://example.org>\n"` — non-http scheme — not an autolink; expect literal text
- `cand.inline.autolink.email-plus-in-local` (§13.2) `"<user+tag@example.org>\n"` — plus in the local part — valid email autolink
- `cand.inline.autolink.bare-ftp-literal` (§13.3) `"see ftp://example.org\n"` — bare ftp scheme — not recognised; expect literal text
- `cand.inline.code.single-space-content` (§10.9) `"` `\n"` — a code span containing a single space — content is non-empty; expect valid code with value ' '
- `cand.inline.code.leading-protective-space` (§10.9) `"`` `x` ``\n"` — protective space around an edge backtick — value is '`x`'
- `cand.inline.code.priority-over-em` (§10.4) `"`*not em*`\n"` — code span has higher priority — the stars inside are literal code content
- `cand.inline.entity.unknown-literal` (§10.8.1) `"&notanentity;\n"` — unknown entity name stays literal text
- `cand.inline.entity.too-many-digits-literal` (§10.8.1) `"&#999999999;\n"` — more than seven decimal digits — not a numeric reference; literal
- `cand.inline.entity.invalid-hex-literal` (§10.8.1) `"&#xZZ;\n"` — invalid hex digits — not a numeric reference; literal
- `cand.inline.entity.newline-literal` (§10.8.1) `"a&NewLine;b\n"` — &NewLine; decodes to LF — not decoded; stays literal, no diagnostic
- `cand.inline.hardbreak.backslash-empty-line-literal` (§10.7) `"a\\\n\\\nb\n"` — a backslash-only line inside a paragraph is literal; surrounding lines give a hard break then soft break
- `cand.inline.escape.leftmost-effective-pair` (§10.8) `"\\*x*\n"` — escaping only the leftmost delimiter neutralises the whole pair — canonical Text('*x*')
- `cand.inline.escape.non-escapable-literal-backslash` (§10.8) `"\\q\n"` — backslash before a non-escapable character stays a literal backslash
- `cand.inline.priority.autolink-vs-email` (§13.2) `"<https://user@example.org>\n"` — a URL containing an @ — the URL scheme wins as a URL autolink, not an email
- `cand.inline.priority.code-vs-reference` (§11.4) `"`[^a]`\n"` — footnote-looking token in a code span — code priority; no reference
- `cand.blocks.heading.six-plus-hashes-literal` (§2.1) `"####### Seven\n"` — seven hashes is beyond the 1-6 ATX range — expect literal paragraph text, not a heading
- `cand.blocks.heading.one-hash-no-space` (§2.1) `"#NoSpace\n"` — opening marker must be followed by a space — expect literal paragraph text
- `cand.blocks.heading.four-leading-spaces-literal` (§2.1) `"    # Title\n"` — four leading spaces exceeds the ATX tolerance — expect literal text or a different block
- `cand.blocks.heading.inline-code-content` (§2.1) `"# A `b` c\n"` — inline code inside heading content — stays as code span, canonical round-trips
- `cand.blocks.heading.emphasis-content` (§2.1) `"# *em* and **strong**\n"` — emphasis delimiters in heading content parse normally
- `cand.blocks.heading.hardbreak-trailing-backslash-literal` (§2.1) `"# Title\\\n"` — a trailing backslash in a heading is a literal backslash (no heading-specific escape) — expect literal backslash
- `cand.blocks.heading.id-hyphen-underscore` (§5.2) `"# Title {#a-b_c}\n"` — id with hyphen and underscore — valid identifier per grammar, round-trips
- `cand.blocks.hr.one-dash-literal` (§2.2) `"--\n"` — two dashes is fewer than three — expect literal text, not an HR
- `cand.blocks.hr.star-single-literal` (§2.2) `"**\n"` — two stars is fewer than three — expect literal text
- `cand.blocks.hr.id-on-same-line` (§2.2) `"--- {#d}\n"` — HR with same-line id — canonical and round-trips
- `cand.blocks.hr.dash-plus-content-literal` (§2.2) `"--- text\n"` — dashes followed by non-ID content — not a valid HR; expect literal text
- `cand.blocks.code.info-uppercase-lang` (§2.4) `"```JS\nx\n```\n"` — language token case is preserved (no case folding) — expect lang 'JS' retained
- `cand.blocks.code.mixed-fence-characters-literal` (§2.4) `"```~~~\nx\n```\n"` — mixed backtick/tilde in one fence run — expect literal text, not a code fence
- `cand.blocks.code.content-with-same-run` (§2.4) `"````\n```\n````\n"` — content containing a shorter backtick run — the four-backtick opener keeps the inner ``` literal
- `cand.blocks.code.content-final-blank` (§2.4) `"```\na\n\n```\n"` — blank content line before closer is structural only — value is 'a\n'
- `cand.blocks.code.empty-content` (§2.4) `"```\n```\n"` — empty code block — ambiguous whether empty value is valid or a syntax error
- `cand.blocks.code.content-leading-spaces-not-stripped` (§2.4) `"```\n  indented\n```\n"` — content indentation is not stripped/re-based — value keeps the two leading spaces
- `cand.blocks.comment.closer-on-later-line` (§2.4.1) `"<!-- first\nsecond\n-->\n"` — multiline comment with closer on its own later line — value preserves the inner newline
- `cand.blocks.comment.empty-value` (§2.4.1) `"<!---->\n"` — empty comment value — valid and round-trips
- `cand.blocks.comment.only-whitespace-value` (§2.4.1) `"<!--   -->\n"` — comment containing only spaces — value preserves the spaces literally
- `cand.blocks.boundary.standalone-dash-line-literal` (§2.1) `"===\n"` — standalone equals line is literal paragraph text (covered) — variant with a dash-only two-dash line
- `cand.blocks.boundary.two-lines-same-paragraph` (§2.5) `"First.\nSecond.\n"` — two consecutive text lines form one paragraph with a soft break
- `cand.blocks.terminal.no-trailing-lf-normalizes` (§7.4) `"Text.\n"` — terminal-newline handling — a document whose final text line has one LF is canonical; probes whether a missing second LF is tolerated
- `cand.blocks.priority.comment-vs-fence` (§15.1) `"<!--\n```\n```\n-->\n"` — comment opener precedes a fence-looking line — comment literal content wins over code fence inside
- `cand.blocks.priority.quote-vs-heading` (§15.1) `"> # inside quote\n"` — quote prefix then heading — quote wins as the structural opener, heading becomes inner content
- `cand.blocks.priority.setext-after-code-literal` (§2.1) `"```\n===\n```\n"` — equals line inside a code fence is literal content, not a Setext attempt
- `cand.blocks.heading.id-attached-to-following-block` (§5.1) `"# Title {#h}\n\nPara.\n"` — same-line heading id stays on the heading; following paragraph unaffected
- `cand.blocks.hr.id-attached-to-following` (§5.1) `"--- {#hr}\n\nPara.\n"` — same-line HR id stays on the HR; following paragraph has no id
- `cand.blocks.comment.empty-doc-comment-only` (§2.4.1) `"<!-- only -->\n"` — document consisting solely of a comment — valid with an empty visible AST
- `cand.blocks.heading.escaped-hash-literal` (§2.1) `"\\# Not heading\n"` — escaped hash at line start — escape neutralises the marker; expect literal text starting with #
- `cand.blocks.hr.escaped-dash-literal` (§2.2) `"\\---\n"` — escaped dash prevents HR recognition — expect literal text
- `cand.footnotes.definition.nested-sup-literal` (§11.3) `"Ref.[^a]\n\n[^a]: Has ^sup^ inside.\n"` — a superscript caret inside a definition — the ^ and [^ must not collide; expect sup literal or parsed normally
- `cand.footnotes.reference.no-space-before` (§11.7) `"Text[^a]\n\n[^a]: note.\n"` — canonical reference directly on the word — round-trips
- `cand.footnotes.reference.double-prefix` (§11.4) `"Text[^^a]\n\n[^a]: note.\n"` — double caret in the reference — identifier grammar is [a-zA-Z0-9_-]+ so ^^ is not a valid id; expect literal text
- `cand.footnotes.reference.space-in-id-literal` (§11.2) `"Text[^a b]\n\n[^a b]: note.\n"` — space in the identifier — not a valid reference; expect literal text
- `cand.footnotes.reference.dash-underscore-id` (§11.2) `"Text[^a-b_c]\n\n[^a-b_c]: note.\n"` — dash and underscore in the identifier — valid and round-trips
- `cand.footnotes.reference.digit-id` (§11.2) `"Text[^1]\n\n[^1]: note.\n"` — numeric identifier — valid
- `cand.footnotes.definition.unused-warning` (§11.3) `"Text.\n\n[^unused]: note.\n"` — a definition with no reference — expect advisory warning, still valid
- `cand.footnotes.definition.duplicate-uppercase-distinct` (§11.3) `"Text[^A]\n\n[^A]: Upper.\n[^a]: Lower.\n"` — definitions differing only in case are distinct — valid
- `cand.footnotes.definition.block-id` (§5.1) `"Ref[^a]\n\n[^a]: note.\n{#fn}\n"` — id suffix on a footnote definition — valid, attaches to the definition
- `cand.footnotes.in-literal-reference` (§11.4) `"`[^a]`\n\n[^a]: note.\n"` — a footnote-looking token inside inline code is literal, not a reference
- `cand.footnotes.in-quote-reference` (§11.3) `"> Quote[^a]\n\n[^a]: note.\n"` — a reference inside a quote — valid; definition stays at document level
- `cand.footnotes.in-list-reference` (§11.3) `"- Item[^a]\n\n[^a]: note.\n"` — a reference inside a list item — valid; definition at document level
- `cand.footnotes.in-container-reference` (§11.3) `"::: Note\nText[^a].\n:::\n\n[^a]: note.\n"` — a reference inside a fenced container — valid
- `cand.footnotes.reference.adjacent-unclosed-caret` (§11.4) `"Text[^a] and then ^b^\n\n[^a]: note.\n"` — a valid reference and a separate superscript caret — the reference wins by priority; sup parses normally
- `cand.footnotes.reference.escaped-bracket-literal` (§11.2) `"Text\\[^a]\n\n[^a]: note.\n"` — an escaped bracket before the reference — escape neutralises the opener; expect literal text, not a reference
- `cand.footnotes.reference.multiline-paragraph` (§11.3) `"First line[^a]\nsecond line.\n\n[^a]: note.\n"` — a reference on the first line of a multi-line paragraph — valid, reference belongs to the paragraph
- `cand.footnotes.reference.at-line-start` (§11.2) `"[^a] at start.\n\n[^a]: note.\n"` — a reference at the start of a line — valid
- `cand.footnotes.reference.inside-heading` (§11.3) `"# Heading[^a]\n\n[^a]: note.\n"` — a reference inside a heading — valid; definition at document level
- `cand.footnotes.definition.id-uppercase-lowercase-distinct` (§11.7) `"Text.\n\n[^A]: upper.\n[^a]: lower.\n"` — two unused definitions differing in case — both valid and independently addressable
- `cand.footnotes.reference.inside-table-cell` (§11.3) `"| A[^a] | B |\n| --- | --- |\n| 1 | 2 |\n\n[^a]: note.\n"` — a reference inside a table cell — valid; definition at document level
- `cand.footnotes.definition.escaped-prefix-literal` (§11.2) `"\\[^a]: literal\n"` — an escaped bracket at the start of a definition line — expect literal text, not a definition
- `cand.footnotes.reference.two-adjacent-references` (§11.3) `"Text[^a][^b]\n\n[^a]: A.\n[^b]: B.\n"` — two adjacent references — both valid and order-preserved by first occurrence
- `cand.footnotes.reference.empty-id-literal` (§11.2) `"Text[^]\n"` — empty identifier inside the bracket — not a valid reference; expect literal text
- `cand.footnotes.reference.double-hyphen-id` (§11.2) `"Text[^a--b]\n\n[^a--b]: note.\n"` — double hyphen inside the identifier — valid per grammar, round-trips
- `cand.footnotes.definition.title-asterisk-content` (§11.3) `"Text[^a]\n\n[^a]: *emphasis* and `code`.\n"` — inline markup inside a definition — valid, emphasis and code parse
- `cand.footnotes.definition.trailing-backslash-literal` (§11.3) `"Text[^a]\n\n[^a]: note\\\n"` — a trailing backslash in a definition is a literal backslash (single-line inline content)
- `cand.footnotes.reference.in-attribution` (§3.17, 11.3) `"> Quote[^a]\n-- Author[^b]\n\n[^a]: A.\n[^b]: B.\n"` — references inside a quote and its attribution line — both valid; attribution is inline content
- `cand.footnotes.reference.bare-caret-no-bracket-literal` (§11.4) `"Text^a and Text[^a]\n\n[^a]: note.\n"` — a bare caret is not a reference (only [^id] is); the bracketed one is a real reference
- `cand.footnotes.reference.in-table-cell` (§11.3, 12.3) `"| a[^f] | b |\n| --- | --- |\n| 1 | 2 |\n\n[^f]: note.\n"` — a footnote reference in a table cell — valid; definition at document level
- `cand.footnotes.definition.inline-code-content` (§11.3) `"Ref[^a]\n\n[^a]: code `x|y` here.\n"` — a pipe inside inline code in a definition — the definition is inline-only and parses code
- `cand.footnotes.reference.two-references-one-definition` (§11.3) `"A[^x] B[^x]\n\n[^x]: note.\n"` — two references to the same identifier — both valid, one definition in output
- `cand.footnotes.reference.with-underscore-only` (§11.2) `"Text[^_]\n\n[^_]: note.\n"` — an identifier that is only an underscore — valid per grammar, round-trips
- `cand.footnotes.reference.escaped-caret-literal` (§11.2) `"Text[^\\^x]\n\n[^x]: note.\n"` — an escaped caret inside the bracket — not a valid identifier; expect literal text
- `cand.footnotes.definition.leading-brackets-content` (§11.3) `"Ref[^a]\n\n[^a]: [bracket] text.\n"` — literal brackets inside definition content — ordinary inline text
- `cand.lists.ordered.space-between-number-dot-invalid` (§9.3.1) `"1 . One\n"` — space between the number and the dot — invalid marker; expect literal text
- `cand.lists.ordered.start-non-one` (§9.3.1) `"3. Three\n4. Four\n"` — list starts at 3 — start is stored; round-trips
- `cand.lists.ordered.start-zero-continues` (§9.3.1) `"0. Zero\n1. One\n"` — start 0 then a sequential 1 — the second item has no value field
- `cand.lists.ordered.deviation-resets-sequence` (§9.3.1) `"1. One\n3. Three\n4. Four\n"` — visible 3 deviates from expected 2 (stores value 3); following implicit numbering continues from 3
- `cand.lists.ordered.repeated-number-deviation` (§9.3.1) `"1. One\n1. Again\n"` — second visible 1 deviates from expected 2 — stores value 1
- `cand.lists.ordered.max-start` (§9.3.1) `"999999999. Nine\n"` — max allowed marker value — valid
- `cand.lists.task.space-in-brackets-invalid` (§9.3.2) `"- [ x ] no\n"` — spaces inside the brackets — not a valid task marker; expect literal
- `cand.lists.task.ordered-task` (§9.3.2) `"1. [ ] ordered\n"` — a task marker on an ordered item — permitted
- `cand.lists.task.nested-task` (§9.3.2) `"- [ ] outer\n  - [x] inner\n"` — task markers in a nested sub-list — permitted
- `cand.lists.task.definition-literal` (§9.3.2) `": [x] term\n"` — task-shaped text on a definition item is ordinary term content, not a task — valid
- `cand.lists.definition.term-group-then-def` (§9.18.2) `": HTML\n: HyperText\n\n  A markup language.\n"` — consecutive terms before one definition group into a term group
- `cand.lists.definition.term-group-at-end` (§9.18.2) `": HTML\n: HyperText\n"` — a trailing term group with no following definition — valid; renders as bare <dt>s
- `cand.lists.indent.skip-back-valid` (§9.5) `"- a\n  - b\n    - c\n- d\n"` — closing multiple levels at once when going back — valid
- `cand.lists.depth.four-levels` (§9.4) `"- a\n  - b\n    - c\n      - d\n"` — four nested levels — no fixed depth limit; expect valid
- `cand.lists.depth.five-levels` (§9.4) `"- a\n  - b\n    - c\n      - d\n        - e\n"` — five nested levels — still valid grammar-wise
- `cand.lists.continuation.soft-break-inside-item` (§9.6) `"- a longer item,\n  continuing here.\n"` — a two-space continuation joins the same paragraph with a soft break
- `cand.lists.structured.comment-child` (§9.8) `"- a\n\n  <!-- note -->\n"` — a comment inside a structured list item — comment is a fenced literal, likely permitted
- `cand.lists.structured.quote-child` (§9.8) `"- a\n\n  > Quote.\n"` — a quote as a structured child — valid
- `cand.lists.structured.blank-closes-leaf-not-item` (§9.8) `"- a\n\n  second paragraph.\n\n  third.\n"` — blank lines close leaf paragraphs but keep the item open at child indentation
- `cand.lists.quote.list-inside-quote` (§3.11) `"> - a\n>   - b\n"` — a nested list inside a quote — quote depth and list depth independent
- `cand.lists.quote.quote-inside-list-inside-quote` (§9.10) `"> - a\n>\n>   > nested quote\n"` — quote-in-list-in-quote recursion — permitted by the ordinary grammar
- `cand.lists.quote.list-item-attribute-placement` (§9.10) `"- Outer\n\n  > Context.\n  >\n  > - Inner\n"` — a list inside a quote inside a structured list item — valid recursion
- `cand.lists.id.list-level-id` (§5.1) `"- a\n- b\n{#list}\n"` — an id suffix after a list — the list node is addressable; expect valid
- `cand.lists.structured.table-child` (§9.8) `"- Data:\n\n  | A | B |\n  | --- | --- |\n  | 1 | 2 |\n"` — a table as a structured child — valid
- `cand.lists.task.mixed-with-plain` (§9.3.2) `"- plain\n- [ ] open\n- [x] done\n"` — plain and task items mix freely in one list
- `cand.lists.structured.math-child` (§9.8) `"- Eq:\n\n  ```math\n  x^2\n  ```\n"` — a math fence as a structured child — permitted as a fenced literal
- `cand.lists.unordered.continuation-then-sibling` (§9.9) `"- a\n  continuation\n- b\n"` — after a continuation, a same-level marker starts a new sibling item
- `cand.math.inline.basic` (§14.2) `"E = $`mc^2`$\n"` — the canonical inline-math form — valid and round-trips
- `cand.math.inline.literal-content` (§14.2) `"$`a_*_b \\frac{1}{2}`$\n"` — content is literal — underscores and backslashes are not parsed as markup
- `cand.math.inline.whitespace-content` (§14.2) `"$`  x  `$\n"` — content with leading/trailing spaces — non-empty, so valid; spaces preserved literally
- `cand.math.inline.dollar-in-content` (§14.2) `"$`a$b`$\n"` — a bare dollar inside the content is not the closer (only `$ closes) — content 'a$b'
- `cand.math.inline.backtick-run-in-content` (§14.2) `"$`x``$\n"` — a backtick run before the closing $ — the first `$ closes; content includes the extra backtick
- `cand.math.inline.at-line-start` (§14.2) `"$`x`$ alone\n"` — inline math at the start of a paragraph line — still inline, not a block
- `cand.math.inline.in-heading` (§14.2) `"# Formula $`x`$\n"` — inline math inside heading content — valid
- `cand.math.inline.in-link-label` (§14.2, 10.5.1) `"[$`x`$](u)\n"` — inline math inside a link label — valid inline content
- `cand.math.inline.in-code-literal` (§10.4) `"`$`x`$`\n"` — a math-looking token inside a code span — code priority; no math node
- `cand.math.inline.dollar-in-code-literal` (§10.4) `"`$100`\n"` — a dollar inside inline code is literal — no math
- `cand.math.inline.adjacent-footnote` (§14.2, 11.3) `"x = $`a`$[^f]\n\n[^f]: note.\n"` — inline math directly followed by a footnote reference — both parse independently
- `cand.math.block.basic` (§14.3) `"```math\n\\left( x \\right)\n```\n"` — a math fence with a multi-line expression — valid MathBlock
- `cand.math.block.multiline` (§14.3) `"```math\na^2\n+\nb^2\n```\n"` — a multi-line math block — internal LFs are part of the value
- `cand.math.block.empty-value` (§14.3) `"```math\n```\n"` — a math fence with no content — ambiguous whether the empty value is valid or an error
- `cand.math.block.info-uppercase-lang` (§2.4) `"```MATH\nx\n```\n"` — uppercase MATH is not the lowercase 'math' token — expect a CodeBlock with lang 'MATH', not MathBlock
- `cand.math.block.inside-quote` (§3.10.3) `"> ```math\n> x^2\n> ```\n"` — a math fence inside a quote region — prefix removed per line, then math parses
- `cand.math.block.inside-container` (§14.3) `"::: Note\n```math\nx^2\n```\n:::\n"` — a math block inside a fenced container — valid
- `cand.math.block.inside-list` (§9.8) `"- Eq:\n\n  ```math\n  x^2\n  ```\n"` — a math block as a structured list child — valid fenced literal
- `cand.math.block.id` (§5.1) `"```math\nx\n```\n{#m}\n"` — an id suffix on a math block — valid
- `cand.math.dollar.bare-dollar-line-literal` (§14.4) `"$\n"` — a lone dollar on a line is literal text, not math
- `cand.math.dollar.pair-literal` (§14.4) `"$5.00$\n"` — $...$ with plain content is literal — inline math requires the backtick span
- `cand.math.dollar.double-pair-literal` (§14.4) `"$$x$$\n"` — double-dollar is not Markanto block math — literal text
- `cand.math.dollar.price-literal` (§14.4) `"Cost: $5 - $3 = $2\n"` — price dollars in running text are literal — no math trigger
- `cand.math.dollar.shell-literal` (§14.4) `"Run $HOME/bin\n"` — shell-variable dollars are literal
- `cand.math.dollar.dollar-in-heading-literal` (§14.4) `"# Budget $5\n"` — a bare dollar in heading text is literal
- `cand.math.block.comment-not-math` (§2.4.1) `"<!-- ```math\nnot math\n-->\n"` — a math-fence-looking line inside a comment is literal comment content
- `cand.math.block.math-not-code` (§14.3) `"```math\nconst x = 1;\n```\n"` — a 'math' info string always yields MathBlock even when the content looks like code
- `cand.math.inline.single-space-content` (§14.2) `"$` `$\n"` — content that is a single space — non-empty, so valid inline math
- `cand.math.inline.two-on-one-line` (§14.2) `"a $`x`$ b $`y`$ c\n"` — two inline-math spans on one line — both parse independently
- `cand.math.inline.in-table-cell` (§12.3, 14.2) `"| A | B |\n| --- | --- |\n| $`x`$ | y |\n"` — inline math inside a table cell — valid single-line inline content
- `cand.math.inline.in-footnote` (§11.3, 14.2) `"Text[^a]\n\n[^a]: value $`x`$.\n"` — inline math inside a footnote definition — valid inline content
- `cand.math.inline.in-emphasis` (§14.2) `"*bold $`x`$ math*\n"` — inline math nested inside emphasis — both constructs parse
- `cand.math.inline.followed-by-emphasis` (§14.2) `"x = $`a`$*note*\n"` — inline math directly followed by emphasis — both parse; no delimiter conflict
- `cand.math.block.anonymous-container` (§2.7, 14.3) `":::\n```math\nx\n```\n:::\n"` — a math block inside an anonymous fenced container — valid
- `cand.math.block.grid-marker-in-math-literal` (§2.7.7) `"::: X\n```math\n--\n==\n```\n:::\n"` — Grid marker lines inside a math fence are literal — no Grid trigger
- `cand.math.dollar.leading-dollar-in-code-literal` (§10.4) `"`$`\n"` — a lone dollar inside a code span is literal — no math
- `cand.math.dollar.dollar-then-code-span` (§14.4, 10.4) `"price is $ `x`\n"` — a bare dollar followed by a code span — the dollar is literal and the code span parses separately
- `cand.math.block.blank-before-closer-literal-lf` (§14.3) `"```math\na\n\n```\n"` — a blank content line before the closer — value is 'a\n' (final LF is structural)
- `cand.math.inline.dollar-within-content-twice` (§14.2) `"$`a$b$c`$\n"` — two bare dollars inside the content are not closers — content 'a$b$c'
- `cand.math.block.fence-info-lowercase-only` (§2.4) `"```Math\nx\n```\n"` — mixed-case 'Math' is not the lowercase token — expect a CodeBlock lang 'Math'
- `cand.tables.delimiter.escaped-pipe-literal` (§12.3) `"| A | B |\n| --- | --- |\n| x\\|y | z |\n"` — an escaped pipe is a literal cell character, not a column boundary
- `cand.tables.delimiter.pipe-in-image-atomic` (§12.3) `"| A | B |\n| --- | --- |\n| ![a|b](i.png) | z |\n"` — a pipe inside a complete image token is atomic — not a column delimiter
- `cand.tables.delimiter.pipe-in-autolink-atomic` (§12.3) `"| A | B |\n| --- | --- |\n| <https://x.org/a|b> | z |\n"` — a pipe inside a bracketed autolink is atomic — not a column delimiter
- `cand.tables.delimiter.pipe-in-link-title` (§12.3) `"| A | B |\n| --- | --- |\n| [x](a \"ti|tle\") | z |\n"` — a pipe inside a link title is part of the atomic link token
- `cand.tables.delimiter.pipe-in-code-atomic` (§12.3) `"| A | B |\n| --- | --- |\n| `a|b` | z |\n"` — a pipe inside an inline-code span is atomic
- `cand.tables.delimiter.backslash-pipe-in-code` (§12.3) `"| A | B |\n| --- | --- |\n| `\\|` | z |\n"` — a backslash-pipe inside a code span is literal code content, not an escape
- `cand.tables.separator.mixed-widths` (§12.4) `"| L | C | R |\n| :--- | :---: | ---: |\n| a | b | c |\n"` — mixed alignment markers across columns — valid, normalised compactly
- `cand.tables.rows.empty-body-cells-valid` (§12.3) `"| A | B |\n| --- | --- |\n|  |  |\n"` — a body row of two empty cells — valid
- `cand.tables.rows.empty-header-cell-valid` (§12.3) `"| A |  |\n| --- | --- |\n| 1 | 2 |\n"` — an empty header cell — valid
- `cand.tables.single-column.basic` (§12.3) `"| A |\n| --- |\n| x |\n"` — a single-column table — valid
- `cand.tables.single-column.align-right` (§12.4) `"| A |\n| ---: |\n| x |\n"` — a single-column right-aligned table — valid
- `cand.tables.single-column.center` (§12.4) `"| A |\n| :---: |\n| x |\n"` — a single-column centred table — valid
- `cand.tables.single-column.body-many` (§12.3) `"| A |\n| --- |\n| 1 |\n| 2 |\n| 3 |\n"` — a single-column table with several body rows — valid
- `cand.tables.cell.angle-exact-literal` (§12.3) `"| < | B |\n| --- | --- |\n| x | y |\n"` — an exact-cell < is ordinary text, not a span — valid
- `cand.tables.cell.caret-exact-literal` (§12.3) `"| ^ | B |\n| --- | --- |\n| x | y |\n"` — an exact-cell ^ in the header is ordinary text, not a span
- `cand.tables.cell.caret-code-literal` (§12.3) `"| A | B |\n| --- | --- |\n| `^` | `<` |\n"` — ^ and < inside code cells are literal code content
- `cand.tables.cell.sup-valid` (§12.3) `"| x^2^ | y |\n| --- | --- |\n| 1 | 2 |\n"` — superscript in a cell — valid single-line inline content
- `cand.tables.cell.math-valid` (§12.3) `"| $`x`$ | y |\n| --- | --- |\n| 1 | 2 |\n"` — inline math in a cell — valid
- `cand.tables.cell.footnote-reference` (§11.3, 12.3) `"| a[^f] | b |\n| --- | --- |\n| 1 | 2 |\n\n[^f]: note.\n"` — a footnote reference in a cell — valid; definition at document level
- `cand.tables.cell.tab-content` (§12.3) `"| a\tb | c |\n| --- | --- |\n| 1 | 2 |\n"` — a tab inside a cell is literal running text — valid
- `cand.tables.cell.inline-image-atomic` (§12.3) `"| ![alt](i.png) | b |\n| --- | --- |\n| 1 | 2 |\n"` — an inline image in a cell is atomic — valid
- `cand.tables.id.valid` (§5.1) `"| A |\n| --- |\n| x |\n{#t}\n"` — an id suffix on a table — valid
- `cand.tables.context.quote-table` (§3.10.4) `"> | A | B |\n> | --- | --- |\n> | 1 | 2 |\n"` — a table inside a quote with the prefix on every line — valid
- `cand.tables.context.list-table` (§9.8) `"- Data:\n\n  | A | B |\n  | --- | --- |\n  | 1 | 2 |\n"` — a table as a structured list child — valid
- `cand.tables.context.container-table` (§2.7) `"::: Note\n| A |\n| --- |\n| x |\n:::\n"` — a table inside a fenced container — valid ordinary content
- `cand.tables.alignment.header-align-vs-separator` (§12.4) `"| A | B |\n| :--- | :---: |\n| 1 | 2 |\n"` — alignment is carried by the separator row only — canonical
- `cand.tables.separator.alignment-pipe-in-cell` (§12.3) `"| A | B |\n| --- | --- |\n| :--- | x |\n"` — a colon-dash sequence in a body cell is ordinary content, not an alignment marker
- `cand.tables.rows.separator-only-document` (§12.3) `"| A |\n| --- |\n"` — a header plus separator with no body rows — valid header-only table
- `cand.tables.rows.leading-indent-before-pipe` (§12.3) `"  | A |\n  | --- |\n  | x |\n"` — leading spaces before every table row — probe whether indentation is tolerated
- `cand.tables.cell.autolink-pipe-atomic` (§12.3) `"| <https://x.org/a|b> | y |\n| --- | --- |\n"` — a bracketed autolink in a header cell with an internal pipe — atomic, one cell
- `cand.tables.cell.star-escaped-literal` (§12.3) `"| \\*x* | y |\n| --- | --- |\n| 1 | 2 |\n"` — an escaped star in a cell neutralises the emphasis pair — literal *x*
- `cand.containers.fenced.title-only-invalid` (§2.7.5) `"::: Note More\nText.\n:::\n"` — a TITLE without a valid TYPE is not representable — here Note is TYPE, More is TITLE; probe single-token title impossible
- `cand.containers.type.hyphen-in-type` (§2.7.4) `"My-Type\n___\n\nContent\n___\n"` — hyphen is not excluded from TYPE — expect valid TYPE 'My-Type'
- `cand.containers.type.dot-in-type` (§2.7.4) `"TL;DR\n___\n\nContent\n___\n"` — period is not excluded from TYPE — expect valid TYPE 'TL;DR'
- `cand.containers.type.underscore-in-type` (§2.7.4) `"Note_Type\n___\n\nContent\n___\n"` — underscore is not excluded from TYPE — expect valid TYPE containing an underscore
- `cand.containers.type.quotes-valid` (§2.7.4) `"Note\"Quoted\"\n___\n\nContent\n___\n"` — quotation marks are ordinary TYPE characters — expect valid TYPE with quotes
- `cand.containers.type.at-sign-valid` (§2.7.4) `"Note@tag\n___\n\nContent\n___\n"` — @ is not excluded from TYPE — expect valid TYPE
- `cand.containers.type.plus-in-type` (§2.7.4) `"C++\n___\n\nContent\n___\n"` — + is not excluded from TYPE — expect valid TYPE 'C++'
- `cand.containers.type.dollar-in-type` (§2.7.4) `"$money\n___\n\nContent\n___\n"` — $ is not excluded from TYPE — expect valid TYPE
- `cand.containers.type.percent-in-type` (§2.7.4) `"100%\n___\n\nContent\n___\n"` — % is not excluded from TYPE — expect valid TYPE
- `cand.containers.type.ampersand-in-type` (§2.7.4) `"Q&A\n___\n\nContent\n___\n"` — ampersand is not excluded from TYPE — expect valid TYPE 'Q&A'
- `cand.containers.lined.contains-fenced-ok` (§2.7.6) `"Outer\n___\n\n::: Note\nNested\n:::\n___\n"` — a fenced container as direct child of a lined container is allowed
- `cand.containers.fenced.grid-only-valid` (§2.7.7) `"::: Grid\nA\n--\nB\n:::\n"` — a Grid as the sole child of a fenced container — valid; columns>1 triggers Grid
- `cand.containers.fenced.grid-not-in-lined` (§2.7.7) `"Outer\n___\n\nA\n--\nB\n___\n"` — Grid marker lines inside a lined container are not Grid syntax — expect them as ordinary content
- `cand.containers.fenced.grid-1x1-not-grid` (§2.7.7) `"::: X\nA\n:::\n"` — a single-cell body is not a semantic Grid (would be ordinary fenced content) — expect plain container
- `cand.containers.fenced.grid-header-1x1-valid` (§2.7.7) `"::: X\nH\n::\nB\n:::\n"` — the :: marker alone commits to Grid; 1x1 header plus 1x1 body is valid
- `cand.containers.fenced.grid-2x2` (§2.7.7) `"::: X\nA\n--\nB\n==\nC\n--\nD\n:::\n"` — a canonical 2x2 Grid with header — valid
- `cand.containers.fenced.grid-marker-in-code-literal` (§2.7.7) `"::: X\n```\n::\n--\n```\n:::\n"` — Grid marker lines inside a code fence are literal, not triggers
- `cand.containers.fenced.grid-caret-only-no-trigger` (§2.7.7) `"::: X\nA\n^\n:::\n"` — ^ alone never triggers Grid recognition — expect ordinary fenced content with a caret line
- `cand.containers.fenced.grid-angle-only-no-trigger` (§2.7.7) `"::: X\nA\n<\n:::\n"` — < alone never triggers Grid recognition — expect ordinary fenced content
- `cand.containers.fenced.id-on-container` (§5.1) `"::: Note\nText.\n:::\n{#n}\n"` — id suffix after a fenced container — valid, attaches to the container
- `cand.containers.lined.id-on-container` (§5.1) `"Exkurs\n___\n\nText.\n___\n{#l}\n"` — id suffix after a lined container — valid, attaches to the container
- `cand.containers.fenced.id-on-grid` (§5.1) `"::: Grid\nA\n--\nB\n:::\n{#g}\n"` — id after a fenced Grid container — attaches to the container (Grid itself has no id field)
- `cand.containers.lined.title-preserves-literal-spacing` (§2.7.5) `"Note a  b\n___\n\nContent\n___\n"` — TITLE is the literal remainder after TYPE — inner double space is preserved verbatim
- `cand.containers.fenced.grid-multiple-fenced-siblings` (§2.7.6) `"Outer\n___\n\n::: A\nOne.\n:::\n\n::: B\nTwo.\n:::\n___\n"` — a lined container with multiple fenced-container siblings — valid
- `cand.containers.fenced.anonymous-mixed-with-content` (§2.7) `":::\nA\n--\nB\n:::\n"` — anonymous fenced container hosting Grid markers — Grid triggers and is valid
- `cand.containers.fenced.grid-markers-in-comment-literal` (§2.7.7) `"::: X\n<!--\n::\n--\n-->\n:::\n"` — Grid marker lines inside a comment are literal, not triggers
- `cand.containers.fenced.grid-rowspan-with-neighbor` (§2.7.7) `"::: X\nA\n--\nB\n==\n^\n--\nC\n:::\n"` — rowspan continuation with a resolved right neighbour — valid geometry
- `cand.quotes.depth-rise-one-at-a-time` (§3.6) `"> L1\n> > L2\n> > > L3\n> > > > L4\n"` — depth rising by one per line — valid
- `cand.quotes.depth-decrease-multiple-ok` (§3.6) `"> L1\n> > L2\n> > > L3\n> L1 again\n"` — decreasing from depth 3 back to depth 1 closes multiple levels at once — valid
- `cand.quotes.depth-decrease-one-ok` (§3.6) `"> L1\n> > L2\n> L1 again\n"` — decreasing by one level — valid
- `cand.quotes.two-spaces-after-prefix` (§3.3) `">  Text\n"` — two spaces after the prefix — canonical wants one; probe whether extra space is trimmed or kept
- `cand.quotes.empty-after-depth-change-ok` (§3.5) `"> L1\n> > L2\n> >\n> > L3\n"` — an empty quote line at the current depth 2 after the change — valid separator
- `cand.quotes.unprefixed-blank-ends-region` (§3.4) `"> Quote.\n\nParagraph.\n"` — an unprefixed blank line ends the quote region
- `cand.quotes.paragraph-boundary-by-depth` (§3.8) `"> L1.\n> > L2.\n> L1 again.\n"` — a depth change ends the current leaf block and begins a new one at the new depth
- `cand.quotes.heading-inside` (§3.10.1) `"> # Heading\n"` — after prefix removal the content is a heading — valid
- `cand.quotes.hr-inside` (§3.10) `"> ---\n"` — an HR inside a quote after prefix removal — valid
- `cand.quotes.emphasis-inside` (§3.8) `"> *em* and **strong**\n"` — inline markup inside a quote — parses normally
- `cand.quotes.hardbreak-inside` (§3.7) `"> line one\\\n> line two\n"` — a prefixed hard break inside the quote — both lines carry the full prefix
- `cand.quotes.code-fence-covered` (§3.10.3) `"> ```js\n> x()\n> ```\n"` — a code fence inside a quote — every line carries the prefix; content excludes it
- `cand.quotes.table-inside` (§3.10.4) `"> | A | B |\n> | --- | --- |\n> | 1 | 2 |\n"` — a table inside a quote — prefix removed per line, then table parses
- `cand.quotes.list-inside` (§3.11) `"> - One\n> - Two\n"` — a list inside a quote — valid
- `cand.quotes.list-inside-nested` (§3.11) `"> - One\n>   - Two\n"` — a nested list inside a quote — list indentation is independent of quote depth
- `cand.quotes.block-media-inside` (§3.10.5) `"> <m video>[Video](film.mp4)</m>\n"` — a block resource inside a quote — recognised as a block-media node
- `cand.quotes.id-after-region` (§3.22) `"> Quote.\n{#q}\n"` — an id on the whole quote region — valid, attaches to the region
- `cand.quotes.id-after-attribution` (§3.22) `"> Quote.\n-- Author\n{#q}\n"` — attribution first, then the region id — canonical order; valid
- `cand.quotes.attribution.basic` (§3.17) `"> Quote.\n-- Author\n"` — a single attribution line right after the region — valid
- `cand.quotes.attribution.markup-content` (§3.17) `"> Quote.\n-- *Author*, from [Source](https://x.org)\n"` — attribution content is inline-parsed — emphasis and links work
- `cand.quotes.attribution.blank-before-not-attribution` (§3.17) `"> Quote.\n\n-- Author\n"` — a blank line before -- makes it ordinary paragraph text
- `cand.quotes.attribution.two-spaces-literal` (§3.17) `"> Quote.\n--  Author\n"` — two spaces after -- is not the exact '-- ' marker — expect literal text
- `cand.quotes.attribution.in-list` (§3.17) `"- Item\n\n  > Quote.\n  -- Author\n"` — an attribution with the same list indentation as the quote stays inside the list item
- `cand.quotes.attribution.after-depth-two` (§3.17) `"> L1\n> > L2\n-- Author\n"` — an attribution after a quote ending at depth 2 — attaches to the whole region
- `cand.quotes.attribution.without-quote-literal` (§3.17) `"-- Author\n"` — -- without a preceding quote region is ordinary paragraph text
- `cand.quotes.math-inside` (§3.10.3, 14.3) `"> ```math\n> x^2\n> ```\n"` — a math fence inside a quote — valid after prefix removal
- `cand.quotes.inside-fenced-container` (§2.7) `"::: Note\n> Quote.\n:::\n"` — a quote inside a fenced container — valid ordinary content
- `cand.quotes.inside-grid-cell` (§2.7.7) `"::: X\n> Quote.\n--\nB\n:::\n"` — a quote inside a Grid cell — cells may contain ordinary non-container blocks
- `cand.quotes.multi-line-with-depth-return` (§3.13) `"> L1\n> > L2\n> > > L3\n> > L2 again\n"` — returning to an outer depth after a deeper block — valid
- `cand.quotes.prefixed-every-line-attribution-excluded` (§3.17) `"> A.\n> B.\n-- Author\n"` — multi-line quote followed by attribution — the attribution carries no quote prefix by design

## noncanonical (165)

_promote as **tolerated valid**, using the `canonical` field as `expect.*.canonical`._

- `cand.resources.image.group-nfc` (§4.6) `"<m group=café>![a](i.jpg)</m>\n"` → `"<m group=café>![a](i.jpg)</m>\n"` — group value is NFC-normalised — combining-accent source becomes composed
- `cand.resources.caption.missing-hardbreak-not-caption` (§4.5) `"![Alt](image.jpg)\n*Caption*\n"` → `"![Alt](image.jpg)\n\n*Caption*\n"` — an adjacent emphasis line without the hard break is not a caption — separate paragraph
- `cand.resources.caption.underscore-carrier` (§4.5) `"![Alt](image.jpg)\\\n_Caption_\n"` → `"![Alt](image.jpg)\\\n<i>Caption</i>\n"` — an underscore-emphasis line as the caption carrier — probe acceptance
- `cand.resources.caption.trailing-spaces` (§4.5) `"![Alt](image.jpg)\\\n*Caption*   \n"` → `"![Alt](image.jpg)\\\n*Caption*\n"` — trailing ASCII whitespace on the caption line — probe removal
- `cand.resources.resource-line.trailing-spaces` (§4.4) `"<m video>[Video](film.mp4)</m>   \n"` → `"<m video>[Video](film.mp4)</m>\n"` — trailing ASCII whitespace on a block resource line — probe removal vs inline reclassification
- `cand.resources.image.line.trailing-spaces` (§4.4) `"![Alt](img.jpg)   \n"` → `"![Alt](img.jpg)\n"` — trailing spaces on a lone image line — probe whether it stays a block resource
- `cand.resources.image.caption-trailing-spaces` (§4.5) `"![Alt](img.jpg)   \\\n*Cap*\n"` → `"![Alt](img.jpg)\\\n<i>Cap</i>\n"` — trailing spaces before the caption hard break — probe acceptance
- `cand.resources.attr.data-order-normalizes` (§4.7) `"<m data-z=1 data-a=2>Text</m>\n"` → `"<m data-a=2 data-z=1>Text</m>\n"` — data-* keys are sorted in ASCII order — canonical is data-a then data-z
- `cand.resources.attr.data-before-core-normalizes` (§4.7) `"<m data-a=1 lang=en>Text</m>\n"` → `"<m lang=en data-a=1>Text</m>\n"` — canonical order puts bare core attributes before data-* — normalises
- `cand.resources.attr.lang-uppercase-normalizes` (§4.6) `"<m lang=EN-US>Text</m>\n"` → `"<m lang=en-us>Text</m>\n"` — lang is ASCII-lowercased — canonical lang=en-us
- `cand.inline.em.empty-pair-invalid` (§10.12) `"****\n"` → `"---\n"` — directly closed empty token — error 10.3; also could be an HR with 4 stars — block priority may claim it as HR instead
- `cand.inline.em.underscore-nested-punct` (§10.3) `"(_em_)\n"` → `"(*em*)\n"` — underscore emphasis inside parentheses — punctuation flanking allows it; normalises to *em*
- `cand.inline.wrapper.strong-then-em-canonical` (§10.6) `"<b><i>x</i></b>\n"` → `"***x***\n"` — Strong containing Em via wrappers — canonical surface is ***x***
- `cand.inline.link.paren-title` (§10.5.1) `"[x](a (t))\n"` → `"[x](a \"t\")\n"` — parenthesised title — tolerated, normalises to double-quoted title
- `cand.inline.link.dest-bare-with-entity` (§10.5.1) `"[a](/x?q=1&amp;r=2)\n"` → `"[a](/x?q=1&r=2)\n"` — entity in the destination is decoded — href '/x?q=1&r=2'
- `cand.inline.link.dest-escaped-paren` (§10.5.1) `"[a](b\\)c)\n"` → `"[a](<b)c>)\n"` — escaped parenthesis in a bare destination — valid, href 'b)c'
- `cand.inline.autolink.mailto-literal` (§13.3) `"<mailto:user@example.org>\n"` → `"\\<mailto:user@example.org>\n"` — mailto is not a recognised scheme — expect literal text
- `cand.inline.autolink.space-inside-literal` (§13.3) `"<https://example.org/a b>\n"` → `"<<https://example.org/a> b>\n"` — space inside the angle brackets — not a valid autolink; expect literal text
- `cand.inline.autolink.email-single-letter-tld-literal` (§13.2) `"<user@example.c>\n"` → `"\\<user@example.c>\n"` — email TLD needs at least two letters — expect literal text
- `cand.inline.autolink.bare-at-line-start` (§13.2a) `"https://example.org/path\n"` → `"<https://example.org/path>\n"` — bare URL at line start — opener position valid; autolink, canonical to <...>
- `cand.inline.autolink.bare-in-parens` (§13.2a) `"(see https://example.org/path)\n"` → `"(see <https://example.org/path>)\n"` — bare URL inside parentheses — autolink; trailing ')' stays literal
- `cand.inline.autolink.bare-trailing-comma` (§13.2a) `"see https://example.org, ok\n"` → `"see <https://example.org>, ok\n"` — bare URL followed by a comma — trailing punctuation trimmed; comma stays literal
- `cand.inline.autolink.bare-trailing-underscore-inside` (§13.2a) `"see https://example.org/a_b\n"` → `"see <https://example.org/a_b>\n"` — underscore inside a bare URL is reserved, not markup — full URL preserved
- `cand.inline.autolink.bare-https-after-punct` (§13.2a) `"word:https://example.org\n"` → `"word:<https://example.org>\n"` — bare URL after a colon — colon is punctuation, so opener position valid; expect autolink
- `cand.inline.entity.named-decodes` (§10.8.1) `"&copy; 2026\n"` → `"© 2026\n"` — named entity decodes to © — canonical text
- `cand.inline.entity.decimal-decodes` (§10.8.1) `"&#65; and &#x42;\n"` → `"A and B\n"` — decimal and hex numeric references decode to A and B
- `cand.inline.entity.tab-decodes` (§10.8.1) `"a&Tab;b\n"` → `"a\tb\n"` — &Tab; decodes to a tab (the one allowed control) — valid
- `cand.inline.entity.numeric-tab-decodes` (§10.8.1) `"a&#9;b\n"` → `"a\tb\n"` — numeric tab reference decodes to a tab — valid
- `cand.inline.entity.emoji-decodes` (§10.8.1) `"&#x1F600;\n"` → `"😀\n"` — emoji code point decodes — valid scalar, canonical text
- `cand.inline.entity.structure-before-decode` (§10.8.1) `"&ast;em&ast;\n"` → `"\\*em*\n"` — decoded stars must not retroactively open emphasis — Text('*em*') with leftmost escape canonical
- `cand.inline.entity.amp-at-line-start` (§10.8.1) `"&amp; leading\n"` → `"& leading\n"` — entity at line start decodes to & — canonical text
- `cand.inline.hardbreak.two-spaces-mid-paragraph` (§7.1) `"a  \nb\n"` → `"a\\\nb\n"` — two trailing spaces in the middle of a paragraph — accepted hard break, normalised to backslash
- `cand.inline.hardbreak.three-trailing-spaces` (§7.1) `"a   \nb\n"` → `"a\\\nb\n"` — three trailing spaces — still a hard break, canonical backslash
- `cand.inline.hardbreak.single-trailing-space-soft` (§7.1) `"a \nb\n"` → `"a\nb\n"` — a single trailing space is not a hard break — soft break, space removed
- `cand.inline.hardbreak.space-before-backslash-normalizes` (§10.7) `"a \\\nb\n"` → `"a\\\nb\n"` — space before the trailing backslash — tolerated, removed; canonical backslash
- `cand.inline.escape.both-escaped-tolerated` (§10.8) `"\\*x\\*\n"` → `"\\*x*\n"` — both delimiters escaped — tolerated normal mode, noncanonical; canonical is \*x*
- `cand.inline.escape.strong-leftmost` (§10.8) `"\\**x**\n"` → `"\\**x*\\*\n"` — escaping the leftmost star neutralises the strong pair — Text('**x**')
- `cand.inline.escape.deletion-leftmost` (§10.8) `"\\~~x~~\n"` → `"\\~~x~\\~\n"` — escaping the leftmost tilde neutralises the deletion pair — Text('~~x~~')
- `cand.inline.escape.double-backslash` (§10.8) `"a\\\\b\n"` → `"a\\b\n"` — a backslash escape produces a literal backslash — Text('a\b')
- `cand.inline.terminal.backslash-only-doc` (§10.7) `"\\\n"` → `"\\\\\n"` — a document that is only a backslash — literal backslash, valid
- `cand.blocks.heading.closing-hash-extra-space` (§2.1) `"# Title  #\n"` → `"# Title\n"` — two spaces before the closing hash run — tolerated and normalised to '# Title'
- `cand.blocks.heading.closing-more-hashes` (§2.1) `"# Title ###\n"` → `"# Title\n"` — closing run longer than the opening marker — tolerated, normalised to '# Title'
- `cand.blocks.heading.closing-fewer-hashes` (§2.1) `"### Title #\n"` → `"### Title\n"` — closing run shorter than the opening marker — tolerated, normalised to '### Title'
- `cand.blocks.heading.tab-after-hash` (§2.1) `"#\tTitle\n"` → `"\\#\tTitle\n"` — tab instead of a space after the marker — ambiguous; expect literal text or a tab tolerance case
- `cand.blocks.heading.three-leading-spaces-normalizes` (§2.1) `"   # Title\n"` → `"# Title\n"` — three leading spaces is the max accepted — normalises to '# Title'
- `cand.blocks.hr.spaced-stars` (§2.2) `"* * *\n"` → `"---\n"` — classic spaced star form — accepted and normalised to '---'
- `cand.blocks.hr.star-trailing-space` (§2.2) `"***   \n"` → `"---\n"` — stars with trailing spaces — tolerated in normal mode, normalised to '---'
- `cand.blocks.hr.dash-tab-separator` (§2.2) `"-\t-\t-\n"` → `"---\n"` — tab-separated dashes — ambiguous whether tabs are allowed between HR chars; expect normalisation or literal
- `cand.blocks.code.info-leading-space-literal` (§2.4) `"``` js\nx\n```\n"` → `"```js\nx\n```\n"` — space before the language token — ambiguous whether it is tolerated or the lang becomes two tokens; expect a syntax error (extra field) or normalisation
- `cand.blocks.code.info-only-whitespace` (§2.4) `"```  \nx\n```\n"` → `"```\nx\n```\n"` — info string is only whitespace — ambiguous between empty lang and extra fields
- `cand.blocks.code.closer-longer-than-opener` (§2.4) `"```\nx\n````\n"` → `"```\nx\n```\n"` — closer longer than the opener — valid in normal mode, normalised to shortest safe fence
- `cand.blocks.code.tilde-opener-content-tilde-literal` (§2.4) `"~~~\n~~~x\n~~~\n"` → `"```\n~~~x\n```\n"` — an info-bearing tilde line inside a tilde fence — the closer must match run length, so this stays content until the final ~~~
- `cand.blocks.comment.closer-then-space-only-ok` (§2.4.1) `"<!-- x -->   \n"` → `"<!-- x -->\n"` — whitespace after the closer is permitted — comment still parses, whitespace removed
- `cand.blocks.boundary.heading-after-paragraph-no-blank` (§2.5) `"Para.\n## Head\n"` → `"Para.\n\n## Head\n"` — heading follows a paragraph without a blank line — valid normal mode, canonical adds a blank line
- `cand.blocks.boundary.hr-after-paragraph-no-blank` (§2.5) `"Para.\n---\n"` → `"Para.\n\n---\n"` — HR follows a paragraph without a blank line — valid normal mode, blank inserted
- `cand.blocks.boundary.code-after-paragraph-no-blank` (§2.5) `"Para.\n```\nx\n```\n"` → `"Para.\n\n```\nx\n```\n"` — code fence follows a paragraph without a blank line — valid normal mode
- `cand.blocks.boundary.comment-after-paragraph-no-blank` (§2.5) `"Para.\n<!-- c -->\n"` → `"Para.\n\n<!-- c -->\n"` — comment follows a paragraph without a blank line — valid normal mode
- `cand.blocks.terminal.multiple-trailing-lf-normalize` (§7.4) `"Text.\n\n\n"` → `"Text.\n"` — several trailing LFs are terminal surface, normalised to one
- `cand.blocks.terminal.trailing-spaces-stripped` (§7.4) `"Text.   \n"` → `"Text.\n"` — trailing structural whitespace on a paragraph line is removed
- `cand.blocks.priority.heading-vs-hr` (§15.1) `"---\n# x\n"` → `"---\n\n# x\n"` — heading after HR without a blank — heading wins as its own block; expect separation
- `cand.blocks.priority.hr-vs-list-item` (§15.1) `"- ---\n"` → `"---\n"` — list marker then an HR run on the same line — list claims the line; expect literal inline text
- `cand.blocks.boundary.comment-between-paragraphs` (§2.5) `"A.\n<!-- c -->\nB.\n"` → `"A.\n\n<!-- c -->\n\nB.\n"` — comment between two paragraphs without blank lines — comment is its own block; separation normalises
- `cand.blocks.boundary.heading-escaped-then-real` (§15.1) `"\\# literal\n# real\n"` → `"\\# literal\n\n# real\n"` — escaped hash line then a real heading — the escaped line is text, the next is a heading
- `cand.footnotes.definition.multiple-spaces-normalize` (§11.2) `"[^x]:  note\n"` → `"\n\n[^x]:  note\n"` — two spaces after the colon — canonical wants exactly one; expect normalisation
- `cand.footnotes.definition.multiline-invalid` (§11.3) `"[^x]: first\n[^y]: second\n"` → `"\n\n[^x]: first\n[^y]: second\n"` — two separate definitions are fine — probes single-line requirement is per-definition not per-block
- `cand.footnotes.definition.block-content-invalid` (§11.3) `"[^x]: note\n\n  continuation\n"` → `"  continuation\n\n[^x]: note\n"` — a block continuation line after a footnote definition — content is inline-only; expect the continuation not to join the definition
- `cand.footnotes.reference.space-before-trivium` (§11.7) `"Text [^a]\n\n[^a]: note.\n"` → `"Text[^a]\n\n[^a]: note.\n"` — ASCII whitespace before a reference is tolerated trivia and discarded — same AST as no-space
- `cand.footnotes.reference.tab-before-trivium` (§11.7) `"Text\t[^a]\n\n[^a]: note.\n"` → `"Text[^a]\n\n[^a]: note.\n"` — tab before a reference — ambiguous whether tab counts as discarded trivia
- `cand.footnotes.reference.inline-markup` (§11.3) `"Text with *em* [^a].\n\n[^a]: note.\n"` → `"Text with *em*[^a].\n\n[^a]: note.\n"` — reference after inline markup — parses normally
- `cand.footnotes.order.referenced-first` (§11.7) `"B[^b] A[^a]\n\n[^a]: A.\n[^b]: B.\n"` → `"B[^b] A[^a]\n\n[^b]: B.\n[^a]: A.\n"` — referenced definitions sort by first-reference order, not source order — expect normalisation to b then a
- `cand.footnotes.order.unused-ascii` (§11.7) `"Text.\n\n[^z]: z.\n[^a]: a.\n"` → `"Text.\n\n[^a]: a.\n[^z]: z.\n"` — unused definitions sort in ASCII order at the end — expect a then z
- `cand.footnotes.order.mixed-referenced-unused` (§11.7) `"X[^y]\n\n[^z]: unused.\n[^y]: used.\n"` → `"X[^y]\n\n[^y]: used.\n[^z]: unused.\n"` — referenced first, then unused in ASCII order — expect y then z
- `cand.footnotes.definition.at-document-end` (§11.3) `"Text[^a].\n\n[^a]: note.\n\nMore.\n"` → `"Text[^a].\n\nMore.\n\n[^a]: note.\n"` — definitions normalise to the end of the document even when written mid-document — expect reorder
- `cand.footnotes.definition.eof-no-trailing-blank` (§11.7) `"Text[^a]\n[^a]: note.\n"` → `"Text[^a]\n\n[^a]: note.\n"` — a definition immediately after the paragraph with no blank — normal mode moves it to the end
- `cand.footnotes.reference.escaped-caret-in-id-literal` (§11.2) `"Text[^a\\]b]\n\n[^a]: note.\n"` → `"Text\\[^a]b]\n\n[^a]: note.\n"` — an escaped bracket inside the reference token — ambiguous; likely not a valid identifier
- `cand.footnotes.reference.only-unused-definitions` (§11.7) `"[^b]: B.\n[^a]: A.\n"` → `"\n\n[^a]: A.\n[^b]: B.\n"` — a document of only unused definitions — valid with warnings, sorted a then b
- `cand.footnotes.definition.adjacent-paragraph-reference` (§11.3) `"Text[^a].\n[^a]: note.\n\nMore text.\n"` → `"Text[^a].\n\nMore text.\n\n[^a]: note.\n"` — definition immediately after the referencing paragraph, then more text — normal mode reorders to the end
- `cand.footnotes.definition.paragraph-after-no-blank` (§11.3) `"Ref[^a]\n\n[^a]: note.\nNext paragraph.\n"` → `"Ref[^a]\n\nNext paragraph.\n\n[^a]: note.\n"` — a paragraph directly after a definition without a blank line — probe the block boundary
- `cand.footnotes.definition.definition-then-blank-then-more` (§11.3) `"Ref[^a]\n\n[^a]: note.\n\nLater.\n"` → `"Ref[^a]\n\nLater.\n\n[^a]: note.\n"` — a definition followed by a blank line and later content — canonical end-of-document placement
- `cand.lists.unordered.mixed-star-plus-dash` (§9.3) `"* a\n+ b\n- c\n"` → `"- a\n- b\n- c\n"` — * and + and - are the same unordered kind — one list, all normalised to '-'
- `cand.lists.ordered.dot-required-normalizes-paren` (§9.3.1) `"1) One\n2) Two\n"` → `"1. One\n2. Two\n"` — paren delimiter normalises to '.' — covered; variant with mixed delimiters
- `cand.lists.ordered.mixed-paren-dot` (§9.3.1) `"1) One\n2. Two\n"` → `"1. One\n2. Two\n"` — mixed ')' and '.' delimiters in one list — tolerated and normalised to '.'
- `cand.lists.ordered.leading-zeros-triple` (§9.3.1) `"001. One\n"` → `"1. One\n"` — leading zeros normalise — '001.' becomes '1.'
- `cand.lists.ordered.zeros-within-range` (§9.3.1) `"0000000001. One\n"` → `"1. One\n"` — ten digits but value 1 after normalisation — within range; expect normalisation to '1.'
- `cand.lists.task.uppercase-x-normalizes` (§9.3.2) `"- [X] done\n"` → `"- [x] done\n"` — uppercase X normalises to lowercase [x]
- `cand.lists.definition.term-then-def-then-term` (§9.18.7) `": Apple\n\n  A fruit.\n\n: Orange\n"` → `": Apple\n\n  A fruit.\n: Orange\n"` — a defined item then a term-only item — the trailing term has no definition
- `cand.lists.definition.old-form-paragraph` (§9.18.1) `"Term\n: Definition\n"` → `"Term\n\n: Definition\n"` — the HMD term-on-own-line pattern is not a definition — first line is a paragraph, second starts a list
- `cand.lists.definition.structured-child-list` (§9.18.3) `": Fruit\n\n  : Apple\n\n    A fruit with a core.\n"` → `": Fruit\n  : Apple\n\n    A fruit with a core.\n"` — a nested definition list inside a definition item — permitted recursive structure
- `cand.lists.kindswitch.ordered-to-unordered` (§9.3.1) `"1. One\n- Two\n"` → `"1. One\n\n- Two\n"` — ordered to unordered switch at the same level — starts a new list
- `cand.lists.kindswitch.unordered-to-ordered` (§9.3.1) `"- One\n1. Two\n"` → `"- One\n\n1. Two\n"` — unordered to ordered switch — starts a new list
- `cand.lists.kindswitch.definition-to-unordered` (§9.18.3) `": Term\n- Item\n"` → `": Term\n\n- Item\n"` — definition to unordered switch — new list, not an error
- `cand.lists.continuation.unindented-ends-list` (§9.9) `"- a\n  continuing\noutside\n"` → `"- a\n  continuing\n\noutside\n"` — a column-0 line after a list continuation ends the list — 'outside' is a new paragraph
- `cand.lists.continuation.column-zero-new-paragraph` (§9.7) `"- a\nb\n"` → `"- a\n\nb\n"` — an unindented text line after a list item — the list ends; 'b' is a new paragraph
- `cand.lists.continuation.deeper-indent-normalizes` (§9.6) `"- a\n    deeper continuation\n"` → `"- a\n  deeper continuation\n"` — four-space continuation — accepted in normal mode, normalised to the two-space baseline
- `cand.lists.structured.sub-list-without-paragraph` (§9.8) `"- a\n\n  - sub\n"` → `"- a\n  - sub\n"` — a sub-list as the first structured child after the intro paragraph — valid
- `cand.lists.structured.code-child-fence-len` (§9.8) `"- Example:\n\n  `````\n  ```\n  `````\n"` → `"- Example:\n\n  ````\n  ```\n  ````\n"` — a code fence inside a list item whose content contains a triple backtick — needs a longer fence
- `cand.lists.continuation.not-enough-indent-simple` (§9.6) `"- a\n b\n"` → `"- a\n\n b\n"` — one-space continuation — CommonMark attaches it as a lazy continuation; expect it joins the paragraph
- `cand.math.inline.escaped-dollar-literal` (§10.8) `"\\$`x`$\n"` → `"\\$`x`\\$\n"` — escaped dollar does not open math — the backtick becomes a code span instead; expect a mixture
- `cand.math.block.tilde-normalizes` (§14.3) `"~~~math\nx^2\n~~~\n"` → `"```math\nx^2\n```\n"` — tilde-fenced math — tolerated in normal mode, normalised to backtick form
- `cand.math.block.longer-tilde` (§14.3) `"~~~~~math\nx^2\n~~~~~\n"` → `"```math\nx^2\n```\n"` — a longer tilde fence with math info — normalised to shortest backtick form
- `cand.math.block.blank-content-line` (§14.3) `"```math\n\n```\n"` → `"```math\n```\n"` — a blank content line before the closer — the LF is structural; value may be empty
- `cand.math.block.info-leading-space` (§2.4) `"``` math\nx\n```\n"` → `"```math\nx\n```\n"` — a space before the math token — ambiguous between an empty-info fence and a two-token error
- `cand.math.block.content-backticks-literal` (§14.3) `"```math\n`a` and ```\n```\n"` → `"````math\n`a` and ```\n````\n"` — backticks inside math content are literal — only the closing fence matters
- `cand.math.block.four-backtick-opener` (§14.3) `"````math\nx\n````\n"` → `"```math\nx\n```\n"` — a longer backtick math fence — normalised to the shortest safe form
- `cand.math.block.leading-spaces` (§2.4) `"  ```math\nx\n  ```\n"` → `"```math\nx\n```\n"` — zero-to-three leading spaces before a math fence — tolerated and removed
- `cand.math.block.grid-cell` (§2.7.7) `"::: X\nA\n```math\nx\n```\n--\nB\n:::\n"` → `"::: X\nA\n\n```math\nx\n```\n--\nB\n:::\n"` — a math block inside a Grid cell — cells may contain ordinary blocks including math
- `cand.math.dollar.escaped-literal` (§10.8) `"\\$100\n"` → `"$100\n"` — an escaped dollar stays a literal dollar
- `cand.math.block.followed-without-blank` (§2.5) `"Para.\n```math\nx\n```\n"` → `"Para.\n\n```math\nx\n```\n"` — a math fence after a paragraph without a blank line — valid normal mode, blank inserted
- `cand.math.inline.in-wrapper` (§10.6, 14.2) `"<i>$`x`$</i>\n"` → `"*$`x`$*\n"` — inline math inside a wrapper — valid; wrapper normalises to *...*
- `cand.math.block.heading-then-fence` (§2.5, 14.3) `"# Math\n```math\nx\n```\n"` → `"# Math\n\n```math\nx\n```\n"` — a math fence after a heading without a blank line — valid normal mode
- `cand.math.block.info-tab` (§2.4) `"```math\t\nx\n```\n"` → `"```math\nx\n```\n"` — a tab after the math info token — ambiguous trailing-field case; expect a syntax error
- `cand.math.block.three-tilde-fence-no-info` (§2.4) `"~~~\nx\n~~~\n"` → `"```\nx\n```\n"` — a tilde fence without info is a CodeBlock, not math — normalises to backtick code
- `cand.math.block.overlong-math-fence` (§14.3) `"``````math\nx\n``````\n"` → `"```math\nx\n```\n"` — a very long math fence — tolerated in normal mode, normalised to shortest safe form
- `cand.tables.delimiter.pipe-in-m-wrapper-atomic` (§12.3) `"| A | B |\n| --- | --- |\n| <m data-x=\"a|b\">t</m> | z |\n"` → `"| A | B |\n| --- | --- |\n| <m data-x=a|b>t</m> | z |\n"` — a pipe inside a quoted MIB attribute is atomic — not a column delimiter
- `cand.tables.separator.too-short-not-table` (§12.3) `"| A | B |\n| -- | -- |\n"` → `"| A | B |\n| --- | --- |\n"` — a separator cell of two dashes is not a valid separator — the lines stay paragraph text
- `cand.tables.separator.single-dash-not-table` (§12.3) `"| A |\n| - |\n"` → `"| A |\n| --- |\n"` — a single dash is not a separator cell — not a table
- `cand.tables.separator.colon-both-sides-short` (§12.3) `"| A |\n| :--: |\n| x |\n"` → `"| A |\n| :---: |\n| x |\n"` — a :--: separator with two dashes — probe whether two dashes are enough for alignment cells
- `cand.tables.separator.extra-dashes` (§12.4) `"| A | B |\n| ----- | ----- |\n| x | y |\n"` → `"| A | B |\n| --- | --- |\n| x | y |\n"` — longer separator dashes normalise to ---
- `cand.tables.separator.double-colon-invalid` (§12.3) `"| A |\n| ::--- |\n"` → `"\\| A |\n| ::--- |\n"` — a double-colon separator cell is not valid alignment grammar — not a table
- `cand.tables.separator.space-in-marker-invalid` (§12.3) `"| A |\n| : --- |\n"` → `"\\| A |\n| : --- |\n"` — a space inside the alignment marker — not a valid separator
- `cand.tables.separator.dash-then-colon-end-invalid` (§12.3) `"| A |\n| ---:: |\n"` → `"\\| A |\n| ---:: |\n"` — a trailing double colon in a separator cell — invalid alignment marker
- `cand.tables.separator.without-outer-pipes` (§12.3) `"A | B\n--- | ---\n1 | 2\n"` → `"| A | B |\n| --- | --- |\n| 1 | 2 |\n"` — a table with no edge pipes at all — tolerated, normalised to canonical edge pipes
- `cand.tables.separator.mixed-edge-pipes` (§12.3) `"| A | B\n| --- | ---\n| 1 | 2\n"` → `"| A | B |\n| --- | --- |\n| 1 | 2 |\n"` — edge pipes only on the left of every row — probe normalisation
- `cand.tables.single-column.no-edge-pipes` (§12.3) `"A\n---\n1\n"` → `"A\n\n---\n\n1\n"` — a single-column table without edge pipes — probe whether the separator line confirms it
- `cand.tables.cell.hardbreak-invalid` (§12.3) `"| a\\\n| --- |\n| b |\n"` → `"| a\\ |\n| --- |\n| b |\n"` — a trailing backslash in a header cell line — cells are break-free; probe how the line is handled
- `cand.tables.context.after-paragraph-no-blank` (§2.5, 12.3) `"Text.\n| A |\n| --- |\n| x |\n"` → `"Text.\n\n| A |\n| --- |\n| x |\n"` — a table immediately after a paragraph line — probe whether confirmation still happens
- `cand.tables.context.after-heading` (§12.3) `"# Heading\n| A |\n| --- |\n| x |\n"` → `"# Heading\n\n| A |\n| --- |\n| x |\n"` — a table immediately after a heading without a blank line — valid normal mode
- `cand.tables.context.followed-by-paragraph` (§12.3) `"| A |\n| --- |\n| x |\nText.\n"` → `"| A |\n| --- |\n| x |\n\nText.\n"` — a paragraph directly after a table — probe separation
- `cand.tables.context.followed-by-hr` (§12.3) `"| A |\n| --- |\n| x |\n---\n"` → `"| A |\n| --- |\n| x |\n\n---\n"` — an HR directly after a table — probe block separation
- `cand.tables.context.quote-prefix-mismatch-not-table` (§12.3) `"> | A | B |\n| --- | --- |\n"` → `"> | A | B |\n\n| --- | --- |\n"` — the separator lacks the quote prefix — different context identity; expect no table
- `cand.tables.context.heading-line-not-table` (§12.3) `"# A\n| --- |\n"` → `"# A\n\n| --- |\n"` — a heading claims the first line, so the separator line cannot confirm a table
- `cand.tables.context.list-opener-not-table` (§12.3) `"- A | B\n--- | ---\n"` → `"- A | B\n\n--- | ---\n"` — the first line is claimed by a list — no retroactive table confirmation
- `cand.tables.context.footnote-opener-not-table` (§12.3) `"[^a]: note\n---\n"` → `"---\n\n[^a]: note\n"` — a footnote-definition-looking first line is claimed by the footnote opener — no table
- `cand.tables.context.after-hr-no-blank` (§12.3) `"---\n| A |\n| --- |\n| x |\n"` → `"---\n\n| A |\n| --- |\n| x |\n"` — a table after an HR without a blank line — valid normal mode
- `cand.tables.alignment.long-form-normalizes` (§12.4) `"| A |\n| :------: |\n| x |\n"` → `"| A |\n| :---: |\n| x |\n"` — an overlong aligned separator normalises to :---:
- `cand.tables.rows.spaces-only-cell-valid` (§12.3) `"|   | B |\n| --- | --- |\n| 1 | 2 |\n"` → `"|  | B |\n| --- | --- |\n| 1 | 2 |\n"` — a cell containing only spaces — an empty cell; valid
- `cand.tables.rows.trailing-space-before-pipe` (§12.3) `"| A  | B |\n| --- | --- |\n| 1 | 2 |\n"` → `"| A | B |\n| --- | --- |\n| 1 | 2 |\n"` — spaces before a pipe inside a cell — trailing cell whitespace; probe trimming
- `cand.tables.rows.space-after-edge-pipe` (§12.3) `"| A | B | \n| --- | --- |\n| 1 | 2 |\n"` → `"| A | B |\n| --- | --- |\n| 1 | 2 |\n"` — a trailing space after the last edge pipe — probe tolerance
- `cand.tables.separator.empty-separator-cell` (§12.3) `"| A | B |\n| | --- |\n"` → `"\\| A | B |\n| | --- |\n"` — an empty separator cell — not a valid alignment marker; probe table rejection
- `cand.tables.cell.escaped-backslash-in-cell` (§12.3) `"| a\\\\b | c |\n| --- | --- |\n| 1 | 2 |\n"` → `"| a\\b | c |\n| --- | --- |\n| 1 | 2 |\n"` — a backslash escape inside a cell is a literal backslash
- `cand.containers.fenced.one-space-before-type-normalizes` (§2.7) `":::  Warning\nContent.\n:::\n"` → `"::: Warning\nContent.\n:::\n"` — two spaces between opener and TYPE — ambiguous; expect normalisation to one space or a syntax error
- `cand.containers.fenced.multiple-spaces-title` (§2.7.5) `"::: Note  Two  spaces\nText.\n:::\n"` → `"::: Note Two  spaces\nText.\n:::\n"` — multiple spaces between TYPE and TITLE — TITLE preserves the literal remainder including inner spacing; canonical header spacing is one space
- `cand.containers.fenced.empty-type-invalid` (§2.7.4) `"::: \nText.\n:::\n"` → `":::\nText.\n:::\n"` — opener with a trailing space but no TYPE — ambiguous between anonymous and invalid; expect anonymous
- `cand.containers.lined.two-blank-lines-normalize` (§2.7.1) `"Exkurs\n___\n\n\nContent\n___\n"` → `"Exkurs\n___\n\nContent\n___\n"` — two blank lines after the opener — normal mode normalises to one
- `cand.containers.lined.anonymous-no-blank` (§2.7.1) `"___\nContent\n___\n"` → `"___\n\nContent\n___\n"` — anonymous lined opener without a blank line — normal mode inserts one
- `cand.containers.lined.closer-longer-ok` (§2.7.3) `"___\n\nContent\n____\n"` → `"___\n\nContent\n___\n"` — closer longer than the opener — valid normal mode, canonical to three underscores
- `cand.containers.fenced.closer-longer-normalizes` (§2.7.3) `":::\nContent\n::::\n"` → `":::\nContent\n:::\n"` — closer longer than opener — valid normal mode, canonical to three colons
- `cand.containers.lined.typed-header-collision-list` (§2.7.1) `"- Header\n___\n\nContent\n___\n"` → `"- Header\n\n___\n\nContent\n___\n"` — a lined-header candidate beginning with a list marker keeps its list meaning, not a lined container
- `cand.containers.lined.typed-header-collision-heading` (§2.7.1) `"# Header\n___\n\nContent\n___\n"` → `"# Header\n\n___\n\nContent\n___\n"` — a lined-header candidate beginning with an ATX heading keeps heading meaning, not a lined container
- `cand.containers.lined.typed-header-collision-hr` (§2.7.1) `"---\n___\n\nContent\n___\n"` → `"---\n\n___\n\nContent\n___\n"` — a lined-header candidate beginning with an HR keeps HR meaning, not a lined container
- `cand.containers.lined.typed-header-collision-quote` (§2.7.1) `"> Header\n___\n\nContent\n___\n"` → `"> Header\n\n___\n\nContent\n___\n"` — a lined-header candidate beginning with a quote marker keeps quote meaning, not a lined container
- `cand.containers.fenced.type-unicode-normalized` (§2.7.4) `"Résumé\n___\n\nText.\n___\n"` → `"Résumé\n___\n\nText.\n___\n"` — TYPE is NFC-normalised — combining-accent source normalises to composed form
- `cand.quotes.compact-depth-two-normalizes` (§3.15.2) `"> L1\n>> L2\n"` → `"> L1\n> > L2\n"` — compact >> after a depth-1 line — normalised to '> > L2'
- `cand.quotes.no-space-content-normalizes` (§3.15.2) `">Text\n"` → `"> Text\n"` — no space after the prefix — normalised to '> Text'
- `cand.quotes.empty-line-trailing-space` (§3.5) `"> \n> Second\n"` → `"> Second\n"` — an empty quote line with a trailing space — canonical has no trailing space; probe normalisation
- `cand.quotes.empty-shallower-after-depth` (§3.5) `"> L1\n> > L2\n>\n> > L3\n"` → `"> L1\n> > L2\n> >\n> > L3\n"` — an empty line at depth 1 between depth-2 lines — probe whether re-entering depth 2 after a shallower empty line is valid
- `cand.quotes.lazy-continuation-not-quote` (§3.7) `"> Quote.\nContinuation.\n"` → `"> Quote.\n\nContinuation.\n"` — an unmarked continuation line ends the quote; the text becomes a sibling paragraph
- `cand.quotes.attribution.second-attribution-literal` (§3.17) `"> Quote.\n-- Author\n-- Second\n"` → `"> Quote.\n-- Author\n\n-- Second\n"` — exactly one attribution is permitted — the second -- line becomes ordinary text
- `cand.quotes.attribution.three-dashes-hr` (§3.17) `"> Quote.\n---\n"` → `"> Quote.\n\n---\n"` — three dashes is not an attribution but an HR — the quote ends and an HR follows
- `cand.quotes.attribution.dash-only-literal` (§3.17) `"> Quote.\n--\n"` → `"> Quote.\n\n--\n"` — -- alone has no space after it — not an attribution; probe literal vs HR
- `cand.quotes.attribution.in-list-unindented-outside` (§3.17) `"- Item\n\n  > Quote.\n-- Author\n"` → `"- Item\n\n  > Quote.\n\n-- Author\n"` — an attribution without the list indentation ends the list and becomes ordinary text
- `cand.quotes.followed-by-paragraph-no-blank` (§3.7) `"> Quote.\nNext.\n"` → `"> Quote.\n\nNext.\n"` — an unmarked line directly after the quote begins a new paragraph outside it
- `cand.quotes.followed-by-heading-no-blank` (§2.5) `"> Quote.\n# Heading\n"` → `"> Quote.\n\n# Heading\n"` — a heading directly after a quote without a blank line — valid normal mode
- `cand.quotes.trailing-space-after-content` (§3.3) `"> Text   \n"` → `"> Text\n"` — trailing structural whitespace inside the quote line — removed
- `cand.quotes.empty-then-attribution-literal` (§3.17) `"> Quote.\n>\n-- Author\n"` → `"> Quote.\n-- Author\n"` — an empty quote line between the content and -- breaks the attribution relation — probe

## rejects-syntax (117)

_promote as **invalid syntax** (pin `diagnostics[0].category = "syntax"`)._

- `cand.resources.video.trailing-text-invalid` (§4.4) `"<m video>[Video](film.mp4)</m> extra\n"` — a typed video line with trailing text — not a whole-line block; probe error
- `cand.resources.typed.unclosed-wrapper-invalid` (§4.4) `"<m video>[Talk](talk.mp4)\\\n*caption*\n"` — a typed wrapper without its closer — expect a syntax error
- `cand.resources.typed.caption-trailing-backslash-no-caption` (§4.5) `"<m video>[Talk](talk.mp4)\\\n</m>\n"` — a trailing hard break with no emphasis line — probe whether the caption is missing and errors
- `cand.resources.caption.unclosed-emphasis-invalid` (§4.5) `"![Alt](image.jpg)\\\n*unclosed\n"` — an unclosed emphasis line after the hard break — expect a syntax error
- `cand.resources.caption.soft-break-invalid` (§4.5) `"![Alt](image.jpg)\\\n*line one\nline two*\n"` — a caption spanning two physical lines — soft breaks are not permitted in a caption; expect error
- `cand.resources.attr.data-duplicate-invalid` (§4.7) `"<m data-x=1 data-x=2>Text</m>\n"` — duplicate data-* keys — expect a syntax error even with different values
- `cand.resources.attr.class-invalid` (§4.6) `"<m class=hero>Text</m>\n"` — class is not a Core attribute — expect a syntax error
- `cand.resources.attr.style-invalid` (§4.6) `"<m video style=dark>[Video](v.mp4)</m>\n"` — style is not a Core attribute — expect a syntax error
- `cand.resources.attr.preview-angle-bare-invalid` (§4.7) `"<m video preview=<a>>[V](v.mp4)</m>\n"` — < and > are excluded from bare values — expect a syntax error
- `cand.resources.attr.empty-bare-invalid` (§4.7) `"<m data-x=>Text</m>\n"` — key= with no value is invalid — the empty string needs ""
- `cand.resources.metadata.kind-among-attrs-invalid` (§4.1) `"<m data-x=1 video>Text</m>\n"` — mKind must be the first token after <m>, never among attributes — expect a syntax error
- `cand.resources.kind.uppercase-invalid` (§4.1) `"<m Video>[V](v.mp4)</m>\n"` — kinds are lowercase-only — expect a syntax error
- `cand.resources.wrapper.unclosed-invalid` (§4.1) `"<m lang=en>Text\n"` — an unclosed MIB wrapper — expect a syntax error
- `cand.resources.wrapper.cross-line-invalid` (§4.7) `"<m lang=en>Text\nmore</m>\n"` — an MIB wrapper spanning a physical line — expect a syntax error
- `cand.resources.attr.quoted-newline-invalid` (§4.7) `"<m data-x=\"a\nb\">Text</m>\n"` — a quoted value spanning a physical line — expect a syntax error
- `cand.inline.em.single-star-literal` (§10.3) `"*\n"` — a lone star on a line is not an HR (needs 3) and not a delimiter — literal text
- `cand.inline.em.nested-same-kind` (§10.4) `"*a *b* c*\n"` — single-star nesting closes stack-aware — ambiguous whether Em>Em same-kind nesting is allowed or a syntax error
- `cand.inline.em.overlapping-invalid` (§10.12) `"**a *b** c*\n"` — overlapping delimiters — expect a syntax error (error 10.2)
- `cand.inline.strong.unclosed-invalid` (§10.12) `"**no end\n"` — unclosed strong — expect syntax error
- `cand.inline.em.unclosed-underscore-invalid` (§10.12) `"_no end\n"` — unclosed underscore emphasis — expect syntax error (no literal fallback)
- `cand.inline.wrapper.unclosed-invalid` (§10.11) `"<b>x\n"` — unclosed strong wrapper — expect syntax error
- `cand.inline.deletion.unclosed-invalid` (§10.12) `"~~no end\n"` — unclosed deletion — expect syntax error
- `cand.inline.obsolete.unclosed-invalid` (§10.12) `"--no end\n"` — unclosed Obsolete — expect syntax error
- `cand.inline.insert.unclosed-invalid` (§10.12) `"++no end\n"` — unclosed insertion (error 10.9) — expect syntax error
- `cand.inline.mark.unclosed-invalid` (§10.12) `"==no end\n"` — unclosed mark — expect syntax error
- `cand.inline.link.extra-text-after-title-invalid` (§10.5.1) `"[x](a \"t\" extra)\n"` — content after the title — malformed group; expect syntax error
- `cand.inline.link.unclosed-title-invalid` (§10.5.1) `"[x](a \"t)\n"` — unclosed title quote — malformed group; expect syntax error
- `cand.inline.link.two-titles-invalid` (§10.5.1) `"[x](a \"t\" 'u')\n"` — two title strings — malformed group; expect syntax error
- `cand.inline.link.empty-string-title-invalid` (§10.5.1) `"[x](a \"\")\n"` — empty-string title is invalid — expect syntax error
- `cand.inline.link.unclosed-angle-dest-invalid` (§10.5.1) `"[x](<a\n"` — unclosed angle destination — expect syntax error
- `cand.inline.link.dest-unbalanced-paren-invalid` (§10.5.1) `"[x](a(b)\n"` — unbalanced parentheses in a bare destination — expect syntax error
- `cand.inline.link.dest-newline-invalid` (§10.5.1) `"[x](<a\nb>)\n"` — destination spanning a physical line — expect syntax error
- `cand.inline.link.label-autolink-invalid` (§10.5.1) `"[<https://inner.org>](https://outer.org)\n"` — autolink inside a link label is a nested anchor — expect syntax error
- `cand.inline.link.label-nested-link-invalid` (§10.5.1) `"[[x](y)](z)\n"` — nested direct link in a label — expect syntax error
- `cand.inline.link.label-footnote-invalid` (§10.5.1) `"[[^n]](a)\n"` — footnote reference in a link label — expect syntax error
- `cand.inline.code.two-backticks-in-content` (§10.9) `"``` `` ```\n"` — content containing two backticks — the triple-backtick delimiter keeps them literal
- `cand.inline.code.unclosed-run-invalid` (§10.9) `"``no end`\n"` — opener run without a matching closer of equal length — expect syntax error
- `cand.inline.code.cross-line-invalid` (§10.9) `"`a\nb`\n"` — inline code crossing a physical line — expect syntax error
- `cand.inline.math.backtick-code-vs-math` (§14.2) `"$`x`\n"` — a dollar-backtick with no closing $ — ambiguous between unclosed inline math and a dollar followed by code; expect an error
- `cand.blocks.heading.empty-closing-invalid` (§2.1) `"## ##\n"` — only closing hashes after the marker leaves empty content — expect syntax error
- `cand.blocks.hr.underscore-not-hr` (§2.2) `"___\n"` — underscore run is no longer an HR — expect it to try lined-container recognition instead
- `cand.blocks.hr.id-on-next-line-literal` (§5.1) `"---\n{#d}\n"` — HR id on the following line instead of same line — ambiguous; heading/HR require same-line id
- `cand.blocks.code.info-with-backtick-invalid` (§2.4) `"```a`b\nx\n```\n"` — backtick in the language token — forbidden for backtick fences; expect syntax error
- `cand.blocks.code.tilde-closer-mismatch` (§2.4) `"~~~\nx\n```\n"` — tilde opener with backtick closer — opener and closer must match; expect unclosed error
- `cand.blocks.code.opener-longer-than-closer` (§2.4) `"````\nx\n```\n"` — closer shorter than the opener — closer must be at least as long; expect unclosed error
- `cand.blocks.comment.four-leading-spaces-literal` (§2.4.1) `"    <!-- x -->\n"` — four leading spaces before the comment marker exceeds the 0-3 tolerance — expect literal text
- `cand.blocks.comment.closer-then-text-invalid` (§2.4.1) `"<!-- x --> y\n"` — visible content after the closer — expect syntax error
- `cand.blocks.comment.content-close-sequence-cannot-escape` (§2.4.1) `"<!-- a --> b -->\n"` — the first --> closes the comment and the rest is invalid visible content — expect syntax error
- `cand.blocks.comment.does-not-render-but-keeps-id-out` (§2.4.1) `"<!-- x -->\n{#c}\n"` — comment followed by an id suffix — CommentBlock has no id field; expect the id to attach to nothing or error
- `cand.blocks.boundary.setext-after-paragraph-diagnostic` (§2.1) `"Para.\n===\n"` — equals line after an in-progress paragraph is a rejected Setext attempt — expect a syntax diagnostic
- `cand.blocks.priority.list-vs-heading-line` (§15.1) `"- # not heading\n"` — a list marker before a hash — list opener claims the line; hash stays inline content
- `cand.blocks.heading.blank-before-id-invalid` (§5.1) `"# Title\n\n{#h}\n"` — id separated from the heading by a blank line — expect syntax error
- `cand.blocks.code.id-after-blank-invalid` (§5.1) `"```\nx\n```\n\n{#c}\n"` — id after a blank line following a code block — expect syntax error
- `cand.blocks.code.escaped-backtick-literal` (§2.4) `"\\```\nx\n"` — escaped backtick at line start prevents fence recognition — expect literal text
- `cand.blocks.priority.blank-then-id-invalid` (§5.1) `"Para.\n\n{#orphan}\n"` — id after a blank line is a syntax error even at document level
- `cand.footnotes.definition.block-id-after-blank-invalid` (§5.1) `"Ref[^a]\n\n[^a]: note.\n\n{#fn}\n"` — definition id after a blank line — expect syntax error
- `cand.footnotes.in-link-label-reference` (§11.3) `"See [the note [^a] here](https://x.org)\n\n[^a]: note.\n"` — a footnote reference inside a link label is a nested anchor — expect syntax error
- `cand.footnotes.definition.unclosed-emphasis-in-content` (§11.3) `"Text[^a]\n\n[^a]: has *unclosed\n"` — an unclosed emphasis inside the definition content — expect syntax error
- `cand.footnotes.reference.in-image-alt-invalid` (§11.3) `"![alt [^a] text](img.png)\n\n[^a]: note.\n"` — a footnote reference inside an image alt is a nested anchor — expect syntax error
- `cand.lists.unordered.no-space-invalid` (§9.3) `"-Item\n"` — no space after the marker — invalid list item; expect literal text or error
- `cand.lists.unordered.two-spaces-invalid` (§9.3) `"-  Item\n"` — two spaces after the marker — invalid; not an acceptable continuation
- `cand.lists.unordered.marker-alone-invalid` (§9.6) `"- \n"` — a marker with no content — empty item; expect syntax error
- `cand.lists.unordered.star-only-invalid` (§9.3) `"*\n"` — a lone star is not a list marker and not an HR — literal text
- `cand.lists.unordered.four-spaces-after-invalid` (§9.3) `"-    Item\n"` — four spaces after the marker — beyond the single-space rule; expect error
- `cand.lists.unordered.marker-tab-invalid` (§9.5) `"-\tItem\n"` — tab after the marker — tabs not permitted in structural indentation; expect error
- `cand.lists.ordered.two-spaces-invalid` (§9.3.1) `"1.  Two\n"` — two spaces after the ordered marker — invalid
- `cand.lists.ordered.marker-alone-invalid` (§9.6) `"1. \n"` — empty ordered item — expect syntax error
- `cand.lists.ordered.over-range-invalid` (§9.3.1) `"1000000000. Too big\n"` — ten digits exceeding the max — expect syntax error
- `cand.lists.task.open-no-content-invalid` (§9.3.2) `"- [ ]\n"` — task marker with no inline content after it — expect syntax error
- `cand.lists.task.empty-content-after-space-invalid` (§9.3.2) `"- [x] \n"` — space after the task marker but no content — expect syntax error
- `cand.lists.definition.colon-no-space-literal` (§9.18.1) `":Term\n"` — colon without a following space is not a definition marker — expect literal paragraph text
- `cand.lists.definition.empty-invalid` (§9.18.1) `": \n"` — empty definition item — expect syntax error
- `cand.lists.definition.two-spaces-invalid` (§9.18.1) `":  Term\n"` — two spaces after the colon — expect syntax error
- `cand.lists.indent.three-space-ambiguous` (§9.4) `"- a\n   - b\n"` — three-space indent is not canonical two-per-level — ambiguous; CommonMark assignment may attach it as a child or continuation
- `cand.lists.indent.four-space-level-jump-invalid` (§9.5) `"- a\n    - b\n"` — four-space indent is a two-level jump (level 1 to 3) — expect syntax error
- `cand.lists.indent.tab-invalid` (§9.5) `"- a\n\t- b\n"` — tab as list indentation — not permitted; expect syntax error
- `cand.math.inline.empty-invalid` (§14.2) `"$``$\n"` — empty inline math — expect syntax error
- `cand.math.inline.cross-line-invalid` (§14.2) `"$`a\nb`$\n"` — inline math across a physical line — expect syntax error
- `cand.math.block.unclosed-invalid` (§14.3) `"```math\nx^2\n"` — an unclosed math fence — expect syntax error
- `cand.math.block.tilde-unclosed-invalid` (§14.3) `"~~~math\nx^2\n"` — an unclosed tilde math fence — expect syntax error
- `cand.math.block.closer-mismatch-invalid` (§14.3) `"```math\nx^2\n~~~\n"` — a tilde closer for a backtick opener — no match; expect unclosed error
- `cand.math.block.info-extra-field-invalid` (§2.4) `"```math extra\nx\n```\n"` — two info-string fields on a math fence — expect syntax error (single-token rule)
- `cand.math.block.id-after-blank-invalid` (§5.1) `"```math\nx\n```\n\n{#m}\n"` — a math id after a blank line — expect syntax error
- `cand.math.dollar.dollar-before-backtick-unclosed` (§14.2) `"$`unterminated\n"` — a dollar-backtick opener with no closer — expect a syntax error, not literal fallback
- `cand.math.inline.closing-sequence-at-start-invalid` (§14.2) `"$``$x`$\n"` — the closing sequence `$ appears immediately after the opener — content empty; expect a syntax error
- `cand.math.inline.empty-then-valid` (§14.2) `"$``$ and $`x`$\n"` — an empty inline math followed by a valid one — expect an error for the empty span
- `cand.math.block.unclosed-with-other-fence` (§14.3) `"```math\n```text\n"` — a different fence inside an open math block does not close it — expect unclosed error
- `cand.tables.delimiter.double-pipe-empty-cell` (§12.3) `"| A | B |\n| --- | --- |\n| a || b |\n"` — two adjacent pipes produce an empty middle cell
- `cand.tables.rows.body-short-invalid` (§12.3) `"| A | B |\n| --- | --- |\n| 1 |\n"` — a body row with too few cells — expect a syntax error
- `cand.tables.rows.body-long-invalid` (§12.3) `"| A | B |\n| --- | --- |\n| 1 | 2 | 3 |\n"` — a body row with too many cells — expect a syntax error
- `cand.tables.cell.unclosed-code-invalid` (§12.3) `"| `oops | x |\n| --- | --- |\n"` — an unclosed code span inside a header cell — expect a syntax error
- `cand.tables.cell.unclosed-link-invalid` (§12.3) `"| [x](a | y |\n| --- | --- |\n"` — an unclosed direct link in a cell — expect a syntax error
- `cand.tables.cell.bad-destination-invalid` (§12.3) `"| [x](a b) | y |\n| --- | --- |\n"` — a link whose destination has an unescaped space — malformed; expect a syntax error
- `cand.tables.id.after-blank-invalid` (§5.1) `"| A |\n| --- |\n| x |\n\n{#t}\n"` — a table id after a blank line — expect a syntax error
- `cand.containers.fenced.no-space-before-type-literal` (§2.7) `":::Warning\nContent.\n:::\n"` — no space between opener and TYPE — ambiguous; expect the line to be an anonymous opener with TYPE as content, or an error
- `cand.containers.lined.closer-must-be-longer-or-equal` (§2.7.3) `"____\n\nContent\n___\n"` — closer shorter than the opener — does not close; expect unclosed error
- `cand.containers.fenced.closer-shorter-invalid` (§2.7.3) `"::::\nContent\n:::\n"` — closer shorter than opener — unclosed, expect syntax error
- `cand.containers.lined.header-with-leading-space-literal` (§2.7.1) `" Exkurs\n___\n\nContent\n___\n"` — leading space before a lined header — container fences have no 0-3 space tolerance; expect literal text
- `cand.containers.lined.escaped-first-underscore-literal` (§2.7.3) `"\\___\n\nContent\n___\n"` — escaped underscore in the opener run prevents fence recognition — expect literal text
- `cand.containers.lined.escaped-middle-underscore-literal` (§2.7.3) `"_\\__\n\nContent\n___\n"` — escaped middle underscore prevents fence recognition — literal text, canonical to \___
- `cand.containers.lined.escaped-last-underscore-literal` (§2.7.3) `"__\\_\n\nContent\n___\n"` — escaped last underscore prevents fence recognition — literal text
- `cand.containers.type.control-invalid` (§2.7.4) `"Bad\u0001Type\n___\n\nContent\n___\n"` — control character in TYPE — excluded; expect syntax error
- `cand.containers.fenced.container-id-after-blank-invalid` (§5.1) `"::: Note\nText.\n:::\n\n{#n}\n"` — container id after a blank line — expect syntax error
- `cand.containers.fenced.opener-not-at-baseline-literal` (§2.7.3) `"  :::\nContent\n:::\n"` — indented fenced opener — no 0-3 space tolerance for container fences; expect literal text
- `cand.quotes.start-depth-two-invalid` (§3.4) `"> > Level 2\n"` — a quote region may not begin at depth 2 — expect syntax error
- `cand.quotes.start-depth-three-invalid` (§3.4) `"> > > Level 3\n"` — a quote region may not begin at depth 3 — expect syntax error
- `cand.quotes.start-compact-depth-two-invalid` (§3.4) `">> Level 2\n"` — compact >> at region start is still depth 2 — expect syntax error
- `cand.quotes.depth-jump-two-invalid` (§3.6) `"> L1\n> > > L3\n"` — a jump from depth 1 to depth 3 rises by two — expect syntax error
- `cand.quotes.depth-jump-three-invalid` (§3.6) `"> L1\n> > > > L4\n"` — a jump from depth 1 to depth 4 — expect syntax error
- `cand.quotes.compact-depth-three-normalizes` (§3.15.2) `"> L1\n>>> L3\n"` — compact >>> after depth 1 — a two-level rise; likely an error, not a normalisation
- `cand.quotes.tab-prefix-invalid` (§3.16) `">\tText\n"` — tab inside the canonical prefix — expect syntax error
- `cand.quotes.content-tab-literal` (§3.9) `"> \tText\n"` — a tab in the content after a proper prefix — tabs are allowed as literal running text; probe
- `cand.quotes.compact-empty-line-literal` (§3.5) `"> Text\n>>\n"` — an empty compact >> after depth 1 — a deeper empty line; expect syntax error
- `cand.quotes.code-missing-prefix-invalid` (§3.16) `"> ```js\nx()\n> ```\n"` — a fence line missing its quote prefix — expect a syntax error
- `cand.quotes.id-after-blank-invalid` (§5.1) `"> Quote.\n\n{#q}\n"` — a region id after a blank line — expect syntax error
- `cand.quotes.id-on-nested-block-invalid` (§5.1) `"> Paragraph.\n> {#inner}\n"` — an id on a block inside a quote is not addressable — expect semantic error
- `cand.quotes.attribution.no-space-after-literal` (§3.17) `"> Quote.\n--Author\n"` — -- with no space is not an attribution marker — expect literal text

## rejects-semantic (43)

_promote as **invalid semantics** (pin `diagnostics[0].category = "semantic"`)._

- `cand.resources.image.empty-group` (§4.6) `"<m group=\"\">![a](i.jpg)</m>\n"` — an empty group value — probe whether group="" is valid or a semantic error
- `cand.resources.image.typed-conflict-invalid` (§4.3) `"<m video>![a](i.jpg)</m>\n"` — an explicit kind plus an image is a type conflict — expect a semantic error
- `cand.resources.video.inline-invalid` (§4.4) `"watch <m video>[Video](film.mp4)</m> now\n"` — video is block-only — inline placement is a semantic error
- `cand.resources.audio.inline-invalid` (§4.4) `"play <m audio>[Audio](sound.mp3)</m> here\n"` — audio is block-only — inline placement is an error
- `cand.resources.embed.inline-invalid` (§4.4) `"see <m embed>[Demo](https://example.org)</m> here\n"` — embed is block-only — inline placement is an error
- `cand.resources.download.group-invalid` (§4.6) `"<m download group=docs>[File](file.zip)</m>\n"` — download has no group attribute — expect a semantic error
- `cand.resources.typed.two-links-invalid` (§4.4) `"<m video>[A](a.mp4)[B](b.mp4)</m>\n"` — a typed wrapper must contain exactly one link — expect a semantic error
- `cand.resources.typed.no-link-invalid` (§4.4) `"<m video>just text</m>\n"` — typed resource with free text instead of a link — expect a semantic error
- `cand.resources.typed.link-extra-text-invalid` (§4.4) `"<m video>[A](a.mp4) extra</m>\n"` — a typed resource with extra text after the link — expect a semantic error
- `cand.resources.caption.empty-invalid` (§4.5) `"![Alt](image.jpg)\\\n**\n"` — an empty emphasis line as the caption — probe error
- `cand.resources.attr.lang-invalid-bcp47` (§4.6) `"<m lang=de__DE>Text</m>\n"` — a malformed BCP 47 tag — expect a semantic error
- `cand.resources.attr.generic-preview-invalid` (§4.6) `"<m preview=x>Text</m>\n"` — preview is not allowed on a kindless generic span — expect a semantic error
- `cand.resources.attr.generic-group-invalid` (§4.6) `"<m group=x>Text</m>\n"` — group is not allowed on a kindless generic span — expect a semantic error
- `cand.resources.metadata.empty-wrapper-invalid` (§4.2) `"<m>Text</m>\n"` — a kindless wrapper with no attribute adds no semantics — expect a semantic error
- `cand.footnotes.definition.indented-literal` (§11.2) `" [^x]: indented\n"` — definition must be at column 0 — a leading space makes it ordinary paragraph text, not a definition
- `cand.footnotes.definition.no-space-after-colon-invalid` (§11.2) `"[^x]:note\n"` — no space after the colon — the content must be non-empty and separated; expect syntax error or literal
- `cand.footnotes.definition.empty-content-invalid` (§11.2) `"[^x]: \n"` — definition with empty content — expect syntax error
- `cand.footnotes.definition.soft-break-error` (§11.3) `"[^x]: first\n[^x]: second\n"` — the definition content must be single-line; this is a duplicate definition — expect semantic error
- `cand.footnotes.definition.nested-reference-invalid` (§11.3) `"Ref.[^a]\n\n[^a]: Nested[^b].\n[^b]: Other.\n"` — a footnote reference inside a footnote definition is forbidden — expect semantic error
- `cand.footnotes.reference.uppercase-id-case-sensitive` (§11.3) `"Text[^A]\n\n[^a]: lower.\n"` — identifiers are case-sensitive — reference A and definition a do not match; expect semantic error
- `cand.footnotes.reference.missing-definition-error` (§11.3) `"Text[^missing]\n"` — a reference with no matching definition — expect semantic error
- `cand.footnotes.definition.duplicate-error` (§11.3) `"Text[^x]\n\n[^x]: First.\n[^x]: Second.\n"` — duplicate definition of the same id — expect semantic error, neither first-wins nor last-wins
- `cand.footnotes.definition.looks-like-in-container` (§11.2) `"::: Note\n[^a]: inside\n:::\n\nText[^a].\n"` — a [^a]: line inside a container is ordinary text but the [^a] token still needs a definition — expect an unresolved-reference semantic error
- `cand.footnotes.definition.looks-like-in-quote` (§11.2) `"> [^a]: inside\n\nText[^a].\n"` — a [^a]: line inside a quote is ordinary text, while Text[^a] is a real reference — expect the reference to need a definition
- `cand.footnotes.definition.inside-grid-cell` (§11.2) `"::: X\n[^a]: cell\n--\nB\n:::\n\nText[^a].\n"` — a [^a]: line inside a Grid cell is ordinary text, while the reference still needs a doc-level definition
- `cand.lists.container.fenced-in-list-invalid` (§9.8) `"- a\n\n  ::: Note\n  Text.\n  :::\n"` — a fenced container inside a list item is forbidden — expect semantic error
- `cand.lists.container.lined-in-list-invalid` (§9.8) `"- a\n\n  Note\n  ___\n\n  Text.\n  ___\n"` — a lined container inside a list item is forbidden — expect semantic error
- `cand.lists.id.contained-invalid` (§5.1) `"- a\n\n  inner.\n  {#inner}\n"` — an id on a block inside a list item is not addressable — expect semantic error
- `cand.containers.lined.nested-lined-invalid` (§2.7.6) `"Outer\n___\n\nInner\n___\n\nContent\n___\n___\n"` — a lined container inside a lined container is forbidden — expect semantic error
- `cand.containers.fenced.nested-fenced-invalid` (§2.7.6) `"::: Outer\n::: Inner\nX\n:::\n:::\n"` — a fenced container inside a fenced container is forbidden — expect semantic error
- `cand.containers.fenced.contains-lined-invalid` (§2.7.6) `"::: Outer\nInner\n___\n\nText\n___\n:::\n"` — a lined container inside a fenced container is forbidden — expect semantic error
- `cand.containers.fenced.grid-rowspan-first-invalid` (§2.7.7) `"::: X\n^\n--\nB\n:::\n"` — ^ continuation in the first row has no slot above — expect semantic error
- `cand.containers.fenced.grid-colspan-first-invalid` (§2.7.7) `"::: X\n<\n--\nB\n:::\n"` — < continuation in the first column has no slot left — expect semantic error
- `cand.containers.fenced.grid-ragged-invalid` (§2.7.7) `"::: X\nA\n--\nB\n==\nC\n:::\n"` — rows with different slot widths — expect semantic error
- `cand.containers.fenced.grid-nonrectangular-invalid` (§2.7.7) `"::: X\nA\n--\n<\n==\n^\n--\nB\n:::\n"` — an L-shaped span ownership is not rectangular — expect semantic error
- `cand.containers.fenced.empty-semantic-error` (§2.7.6) `":::\n:::\n"` — empty fenced container — expect semantic error (non-empty children invariant)
- `cand.containers.lined.empty-semantic-error` (§2.7.6) `"___\n\n___\n"` — empty lined container — expect semantic error
- `cand.containers.fenced.container-inside-quote-invalid` (§2.7.6) `"> ::: Note\n> Text.\n> :::\n"` — a fenced container inside a quote is forbidden — expect semantic error
- `cand.containers.lined.container-inside-list-invalid` (§2.7.6) `"- item\n\n  Exkurs\n  ___\n\n  Text.\n  ___\n"` — a lined container inside a list item is forbidden — expect semantic error
- `cand.containers.fenced.grid-cell-container-invalid` (§2.7.7) `"::: Grid\n::: Inner\nX\n:::\n--\nB\n:::\n"` — a container inside a Grid cell is forbidden — expect semantic error
- `cand.quotes.container-fenced-invalid` (§3.10) `"> ::: Warning\n> Content.\n> :::\n"` — a fenced container inside a quote is forbidden — expect semantic error
- `cand.quotes.container-lined-invalid` (§3.10) `"> Exkurs\n> ___\n>\n> Content.\n> ___\n"` — a lined container inside a quote is forbidden — expect semantic error
- `cand.quotes.container-anonymous-lined-invalid` (§3.10) `"> ___\n>\n> Content.\n> ___\n"` — an anonymous lined container inside a quote is forbidden — expect semantic error

## rejects-noncanonical (0)

_review — a noncanonical rejection in normal mode is unusual._


## resource (0)

_review — a small candidate should not hit a budget._


