# Migration evolution gate — v0.5.3 → 0.1.0

> Regenerated by `test/migrate/migration-evolution-gate.test.ts`. Do not edit by hand.

Source corpus: `test/migrate/v053-corpus/` (frozen snapshot, sha256 `607c12cb4d27eb41…`).
Each of the 265 valid v0.5.3 cases migrates to a valid 0.1.0 document **or** carries an explicit
`dropped` / `unrepresentable` / `review` diagnostic that names a construct-level obstruction; no v0.5.3
construct vanishes from the migrated AST without a diagnostic; every `mapped` migration round-trips.

| Outcome | Cases |
|---|---:|
| clean (no diagnostic, valid 0.1.0) | 221 |
| `mapped` (rewritten, round-trips) | 26 |
| `review` (meaning-changing rewrite flagged) | 11 |
| `dropped` (obsolete field/node removed) | 1 |
| `unrepresentable` (no 0.1.0 form; kept local or removed) | 6 |
| **total** | **265** |

Migrated output: 259 / 265 parse to a valid, non-empty 0.1.0 document (0 of them parse but not back to the intended tree); 6 have no valid non-empty output (empty, or an invalid surface). Every case outside "clean" / "mapped" carries a construct-level named diagnostic; no `unrepresentable` diagnostic is generic.

